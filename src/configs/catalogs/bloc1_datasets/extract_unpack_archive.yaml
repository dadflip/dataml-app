id: extract_unpack_archive
name: 'Extract: Décompresser Archive'
description: Dézippe ou dé-tar un fichier d'archive téléchargé.
when_to_use: "Quand les données sources sont compressées (.zip, .tar, .tgz) et doivent être extraites avant utilisation."
use_cases:
  - "Décompression de jeux de données téléchargés depuis Kaggle ou un autre portail open data."
  - "Récupération d'un export massif compressé par un outil tiers."
pros:
  - "Gère automatiquement plusieurs formats standards d'archivage."
  - "Permet de réduire l'espace disque de transfert."
cons:
  - "Nécessite un espace disque suffisant pour les fichiers décompressés."
  - "Temps de traitement supplémentaire avant lecture des données."
category: 1. Extract
tags:
- extract
- unzip
- tar
- archive
code_template: |
  import os
  import tarfile
  import zipfile
  import time
  import pandas as pd

  # [PARAM: ARCHIVE_PATH | file]
  ARCHIVE_PATH = "./data/raw/dataset.zip"
  # [/PARAM]

  # [PARAM: EXTRACT_DIR | dir]
  EXTRACT_DIR = "./data/extracted"
  # [/PARAM]

  print(f"[EXTRACT] Décompression de {ARCHIVE_PATH} vers {EXTRACT_DIR}...")
  os.makedirs(EXTRACT_DIR, exist_ok=True)

  try:
      start_time = time.time()
      if ARCHIVE_PATH.endswith(".zip"):
          with zipfile.ZipFile(ARCHIVE_PATH, 'r') as zip_ref:
              zip_ref.extractall(EXTRACT_DIR)
      elif ARCHIVE_PATH.endswith((".tar", ".tar.gz", ".tgz")):
          with tarfile.open(ARCHIVE_PATH, "r:*") as tar_ref:
              tar_ref.extractall(path=EXTRACT_DIR)
      else:
          raise ValueError("Format d'archive non supporté (.zip, .tar, .tgz uniquement)")
          
      duration = time.time() - start_time
      print(f"[EXTRACT] Succès. Contenu extrait en {duration:.2f}s.")
  except Exception as e:
      print(f"[ERREUR EXTRACT] Impossible de décompresser l'archive : {str(e)}")
      raise e

  df = pd.DataFrame()
  target_name = ""
  task_type = "unknown"
  meta = {"source": "archive_extract", "path": EXTRACT_DIR}

illustration_mermaid: |
  flowchart LR
      A[/"📄 ARCHIVE_PATH"/] --> B{"extractall()"}
      C[/"🎯 TARGET_COL"/] --> D{"Check TARGET_COL"}
      B --> E(["📊 df (DataFrame)"])
      E --> D
      D --> F(["🎯 target_name"])
      D --> G(["⚙️ task_type"])
      D --> H(["ℹ️ meta (source, path)"])
      style A fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
      style C fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
      style B fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style D fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style E fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
      style F fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style G fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style H fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff

_metadata:
  bloc: 1
  section: 1_extract
