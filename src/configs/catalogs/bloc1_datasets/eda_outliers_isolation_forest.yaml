id: eda_outliers_isolation_forest
name: 'EDA: Détection Outliers (Isolation Forest)'
description: Repère visuellement les anomalies multivariées via Isolation Forest.
category: 3. Exploratory Data Analysis
tags:
- eda
- outliers
- tabular
- sklearn
code_template: "import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot\
  \ as plt\nimport seaborn as sns\nfrom sklearn.ensemble import IsolationForest\n\n\
  # [PARAM: CONTAMINATION | number]\nCONTAMINATION = 0.05\n# [/PARAM]\n\nnum_df =\
  \ df.select_dtypes(include=[np.number]).dropna()\nif len(num_df) > 50 and len(num_df.columns)\
  \ >= 2:\n    iso = IsolationForest(contamination=CONTAMINATION, random_state=42)\n\
  \    preds = iso.fit_predict(num_df)\n    \n    cols = num_df.columns[:2]\n    \n\
  \    plt.figure(figsize=(8, 6))\n    scatter = plt.scatter(num_df[cols[0]], num_df[cols[1]],\
  \ c=preds, cmap='coolwarm', alpha=0.7)\n    plt.title(f\"Isolation Forest : Outliers\
  \ sur {cols[0]} vs {cols[1]}\")\n    plt.xlabel(cols[0])\n    plt.ylabel(cols[1])\n\
  \    # -1 (rouge) = outlier, 1 (bleu) = inlier\n    plt.show()\n    print(f\"[EDA]\
  \ {sum(preds == -1)} outliers détectés sur {len(num_df)} lignes ({(sum(preds ==\
  \ -1)/len(num_df)*100):.1f}%).\")\nelse:\n    print(\"[EDA] Dataset inadapté pour\
  \ Isolation Forest.\")\n"
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eda_outliers_isolation_forest" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eda_outliers_isolation_forest" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eda_outliers_isolation_forest)" stroke="#64748b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#64748b" opacity="0.15" filter="url(#glow_eda_outliers_isolation_forest)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#94a3b8" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 L100 50 M60 20 L60 80" />  </g>  <path d="M 120 50 L 550 50" stroke="#64748b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#64748b"/>  <circle cx="350" cy="50" r="4" fill="#64748b"/>  <circle cx="500" cy="50" r="4" fill="#64748b"/></svg>
  width="400" height="200" fill="#0f172a"/><circle cx="200" cy="100" r="40" fill="#3b82f6"
  opacity="0.3"/><circle cx="190" cy="90" r="4" fill="#3b82f6"/><circle cx="210" cy="110"
  r="4" fill="#3b82f6"/><circle cx="180" cy="105" r="4" fill="#3b82f6"/><circle cx="205"
  cy="85" r="4" fill="#3b82f6"/><circle cx="80" cy="60" r="6" fill="#ef4444"/><circle
  cx="320" cy="150" r="6" fill="#ef4444"/><line x1="160" y1="80" x2="85" y2="65" stroke="#ef4444"
  stroke-width="1" stroke-dasharray="2,2"/><line x1="240" y1="120" x2="315" y2="145"
  stroke="#ef4444" stroke-width="1" stroke-dasharray="2,2"/></svg>
_metadata:
  bloc: 1
  section: 3_eda
