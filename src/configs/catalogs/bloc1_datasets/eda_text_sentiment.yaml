id: eda_text_sentiment
name: 'EDA: Distribution des Sentiments'
description: Analyse la polarité des textes via TextBlob.
category: 3. Exploratory Data Analysis
tags:
- eda
- text
- nlp
- sentiment
code_template: |
  import pandas as pd
  import matplotlib.pyplot as plt
  import seaborn as sns
  from textblob import TextBlob
  
  # [PARAM: TEXT_COL | string]
  TEXT_COL = "text"
  # [/PARAM]
  
  if TEXT_COL in df.columns:
      print("[EDA] Calcul des scores de sentiment en cours...")
      polarities = df[TEXT_COL].dropna().astype(str).apply(lambda x: TextBlob(x).sentiment.polarity)
      
      plt.figure(figsize=(8, 5))
      sns.histplot(polarities, bins=30, kde=True, color='teal')
      plt.title(f"Distribution de Polarité des Sentiments - {TEXT_COL}")
      plt.xlabel("Polarité (-1 = Négatif, 1 = Positif)")
      plt.show()
  else:
      print(f"[EDA] Colonne {TEXT_COL} introuvable.")
illustration_mermaid: |
    graph TD
      DF([DataFrame]) --> Check{Check TEXT_COL}
      Check -- Valid --> Blob{{TextBlob}}
      Blob -- "polarity" --> Scores[Extract Polarities]
      Scores --> Plot[sns.histplot]
      Plot --> Show[plt.show]
      Check -- Missing --> Err[Print Error]
      style Blob fill:#d946ef,stroke:#c026d3,stroke-width:2px,color:#fff
      style Scores fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style Plot fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
_metadata:
  bloc: 1
  section: 3_eda
