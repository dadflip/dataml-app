id: "load_pickle"
name: "Load: Python Pickle (.pkl)"
description: "Désérialise un objet Python binaire natif (ex: DataFrame, Modèle, Dictionnaire)."
category: "2. Load"
tags: ["load", "pickle", "python", "binary"]
code_template: |
  import time
  import os

  # [PARAM: FILE_PATH | file]
  FILE_PATH = "./data/raw/data.pkl"
  # [/PARAM]

  print(f"[LOAD] Désérialisation du fichier Pickle : {FILE_PATH}...")
  if not os.path.exists(FILE_PATH):
      raise FileNotFoundError(f"[ERREUR LOAD] Fichier Pickle introuvable : {FILE_PATH}")

  try:
      start_time = time.time()
      import pickle
      import pandas as pd
      
      with open(FILE_PATH, 'rb') as f:
          data = pickle.load(f)
          
      duration = time.time() - start_time
      
      # Tente de l'exposer comme un DataFrame si applicable
      df = data if isinstance(data, pd.DataFrame) else None
      
      print(f"[LOAD] Succès Pickle. Objet de type '{type(data).__name__}' chargé en {duration:.2f}s.")
  except Exception as e:
      print(f"[ERREUR LOAD] Impossible de charger le fichier .pkl : {str(e)}")
      raise e

  target_name = None
  task_type = "unknown"
  meta = {"format": "pickle", "object_type": type(data).__name__}
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_load_pickle" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_load_pickle" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_load_pickle)" stroke="#3b82f6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#3b82f6" opacity="0.15" filter="url(#glow_load_pickle)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#60a5fa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M10 30 L50 70 L90 30" />  </g>  <path d="M 120 50 L 550 50" stroke="#3b82f6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#3b82f6"/>  <circle cx="350" cy="50" r="4" fill="#3b82f6"/>  <circle cx="500" cy="50" r="4" fill="#3b82f6"/></svg>
_metadata:
  bloc: 1
  section: "2_load"
