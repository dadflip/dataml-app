id: "load_webdataset"
name: "Load: WebDataset (.tar streaming)"
description: "Charge de très gros datasets encapsulés dans des fichiers .tar de manière distribuée (standard PyTorch)."
category: "2. Load"
tags: ["load", "webdataset", "pytorch", "streaming"]
code_template: |
  import glob
  import os
  import time

  # [PARAM: URL_OR_PATH | string]
  URL_OR_PATH = "./data/extracted/shards/{00000..00099}.tar"
illustration_mermaid: |
    graph LR
      Source[(Source)] -->|Load| Node([Extract Data])
      style Source fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style Node fill:#eff6ff,stroke:#3b82f6,stroke-width:2px

_metadata:
  bloc: 1
  section: "2_load"
