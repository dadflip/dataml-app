id: "load_torchvision"
name: "Load: Torchvision Dataset"
description: "Charge un dataset canonique d'imagerie (CIFAR, MNIST, ImageNet) via torchvision."
category: "2. Load"
tags: ["load", "torchvision", "pytorch", "images"]
code_template: |
  import time
  import os

  # [PARAM: DATASET_NAME | string]
  DATASET_NAME = "CIFAR10"
  # [/PARAM]
  
  # [PARAM: DATA_DIR | dir]
  DATA_DIR = "./data/raw/torchvision"
  # [/PARAM]

  print(f"[LOAD] Téléchargement/Chargement du dataset Torchvision '{DATASET_NAME}'...")
  os.makedirs(DATA_DIR, exist_ok=True)

  try:
      start_time = time.time()
      import torchvision.datasets as dsets
      
      # Récupération dynamique de la classe du dataset
      dataset_class = getattr(dsets, DATASET_NAME)
      
      # root et download sont les paramètres standards de torchvision
      dataset = dataset_class(root=DATA_DIR, train=True, download=True)
      
      duration = time.time() - start_time
      print(f"[LOAD] Succès Torchvision. {len(dataset)} éléments chargés en {duration:.2f}s.")
  except AttributeError:
      print(f"[ERREUR LOAD] Le dataset '{DATASET_NAME}' n'existe pas dans torchvision.datasets.")
      raise
  except ImportError:
      print("[ERREUR LOAD] Le module 'torchvision' est requis.")
      raise
  except Exception as e:
      print(f"[ERREUR LOAD] Erreur Torchvision : {str(e)}")
      raise e

  df = None
  target_name = "target"
  task_type = "computer_vision_classification"
  meta = {"format": "torchvision", "dataset": DATASET_NAME, "size": len(dataset)}
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_load_torchvision" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_load_torchvision" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_load_torchvision)" stroke="#3b82f6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#3b82f6" opacity="0.15" filter="url(#glow_load_torchvision)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#60a5fa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M10 30 L50 70 L90 30" />  </g>  <path d="M 120 50 L 550 50" stroke="#3b82f6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#3b82f6"/>  <circle cx="350" cy="50" r="4" fill="#3b82f6"/>  <circle cx="500" cy="50" r="4" fill="#3b82f6"/></svg>
_metadata:
  bloc: 1
  section: "2_load"
