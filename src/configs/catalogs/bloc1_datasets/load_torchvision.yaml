id: "load_torchvision"
name: "Load: Torchvision Dataset"
description: "Charge un dataset canonique d'imagerie (CIFAR, MNIST, ImageNet) via torchvision."
category: "2. Load"
tags: ["load", "torchvision", "pytorch", "images"]
code_template: |
  import time
  import os

  # [PARAM: DATASET_NAME | string]
  DATASET_NAME = "CIFAR10"
illustration_mermaid: |
    graph LR
      Source[(Source)] -->|Load| Node([Extract Data])
      style Source fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style Node fill:#eff6ff,stroke:#3b82f6,stroke-width:2px

_metadata:
  bloc: 1
  section: "2_load"
