id: "load_sqlite"
name: "Load: Base SQLite (.db)"
description: "Exécute une requête SQL directement sur un fichier de base SQLite local."
category: "2. Load"
tags: ["load", "sqlite", "sql", "db"]
code_template: |
  import sqlite3
  import pandas as pd
  import time
  import os

  # [PARAM: FILE_PATH | file]
  FILE_PATH = "./data/raw/database.db"
  # [/PARAM]
  
  # [PARAM: QUERY | string]
  QUERY = "SELECT * FROM my_table"
  # [/PARAM]
  
  # [PARAM: TARGET_COL | string]
  TARGET_COL = "target"
  # [/PARAM]

  print(f"[LOAD] Connexion à la base SQLite {FILE_PATH}...")
  if not os.path.exists(FILE_PATH):
      raise FileNotFoundError(f"[ERREUR LOAD] Base de données introuvable : {FILE_PATH}")

  try:
      start_time = time.time()
      conn = sqlite3.connect(FILE_PATH)
      df = pd.read_sql_query(QUERY, conn)
      conn.close()
      
      duration = time.time() - start_time
      print(f"[LOAD] Succès SQLite. Résultat de la requête ({df.shape[0]} lignes) chargé en {duration:.2f}s.")
  except Exception as e:
      print(f"[ERREUR LOAD] Erreur SQLite : {str(e)}")
      raise e

  target_name = TARGET_COL if TARGET_COL and TARGET_COL in df.columns else None
  task_type = "unknown"
  meta = {"format": "sqlite", "shape": df.shape}
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_load_sqlite" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_load_sqlite" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_load_sqlite)" stroke="#3b82f6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#3b82f6" opacity="0.15" filter="url(#glow_load_sqlite)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#60a5fa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M10 30 L50 70 L90 30" />  </g>  <path d="M 120 50 L 550 50" stroke="#3b82f6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#3b82f6"/>  <circle cx="350" cy="50" r="4" fill="#3b82f6"/>  <circle cx="500" cy="50" r="4" fill="#3b82f6"/></svg>
_metadata:
  bloc: 1
  section: "2_load"
