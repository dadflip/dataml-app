id: eda_audio_spectrogram_plot
name: 'EDA: Spectrogramme (Matplotlib)'
description: Génère un spectrogramme visuel via matplotlib sans torchaudio.
category: 3. Exploratory Data Analysis
tags:
- eda
- audio
- multimedia
- spectrogram
code_template: |
  import matplotlib.pyplot as plt

  # [PARAM: SAMPLE_RATE | number]
  SAMPLE_RATE = 16000
  # [/PARAM]

  if 'waveform' in locals():
      # Convertir stereo en mono pour la visualisation
      if waveform.shape[0] > 1:
          signal = waveform.mean(dim=0).numpy()
      else:
          signal = waveform[0].numpy()
          
      plt.figure(figsize=(12, 5))
      plt.specgram(signal, Fs=SAMPLE_RATE, cmap='magma')
      plt.title("Spectrogramme du signal audio")
      plt.ylabel("Fréquence (Hz)")
      plt.xlabel("Temps (s)")
      plt.colorbar(format='%+2.0f dB')
      plt.show()
  else:
      print("[EDA] Tensor 'waveform' introuvable.")
illustration_mermaid: |
    graph TD
      A([Tensor 'waveform']) --> B{Exists in locals?}
      B -- Yes --> C{Channels > 1?}
      C -- Yes --> D[Mean across channels<br>stereo to mono]
      C -- No --> E[Extract Channel 0]
      D --> F[/Convert to numpy/]
      E --> F
      F --> G([Mono Signal])
      G --> H{{Plot specgram<br>Fs=SAMPLE_RATE, cmap='magma'}}
      H --> I[Add Title, Labels, Colorbar]
      I --> J([Display Spectrogram])
      B -- No --> K[/Print Error Message/]
      style A fill:#4F46E5,color:#fff
      style B fill:#F59E0B,color:#fff
      style C fill:#F59E0B,color:#fff
      style H fill:#10B981,color:#fff

_metadata:
  bloc: 1
  section: 3_eda
