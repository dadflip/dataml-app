id: load_fasta
name: 'Load: Séquences ADN FASTA'
description: Parse un fichier de séquences génétiques FASTA via Biopython.
category: 2. Load
tags:
- load
- fasta
- bioinformatics
- dna
code_template: |
  import time
  import os
  import pandas as pd

  # [PARAM: FILE_PATH | file]
  FILE_PATH = "./data/raw/sequence.fasta"
  # [/PARAM]

  print(f"[LOAD] Parsing FASTA : {FILE_PATH}...")
  if not os.path.exists(FILE_PATH):
      raise FileNotFoundError(f"[ERREUR LOAD] Fichier FASTA introuvable : {FILE_PATH}")

  try:
      start_time = time.time()
      from Bio import SeqIO
      
      records = list(SeqIO.parse(FILE_PATH, "fasta"))
      count = len(records)
      
      duration = time.time() - start_time
      print(f"[LOAD] Succès FASTA. {count} séquences chargées en {duration:.2f}s.")
  except ImportError:
      print("[ERREUR LOAD] Le module 'biopython' est requis (pip install biopython).")
      raise
  except Exception as e:
      print(f"[ERREUR LOAD] Erreur de parsing FASTA : {str(e)}")
      raise e

  df = pd.DataFrame([{"id": r.id, "sequence": str(r.seq)} for r in records])
  target_name = ""
  task_type = "nlp_sequence"
  meta = {"format": "fasta", "num_sequences": count}
illustration_mermaid: |
  flowchart LR
      A[/"📄 FILE_PATH (.fasta)"/] --> B{"SeqIO.parse()"}
      C[/"🎯 TARGET_COL (N/A)"/] --> D{"Set Attributes"}
      B --> E(["📊 df (DataFrame)"])
      E --> D
      D --> F(["🎯 target_name = ''"])
      D --> G(["⚙️ task_type = 'nlp_sequence'"])
      D --> H(["ℹ️ meta (format, num_sequences)"])
      style A fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
      style C fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
      style B fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style D fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style E fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
      style F fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style G fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style H fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff

_metadata:
  bloc: 1
  section: 2_load
