id: "load_yaml"
name: "Load: Configuration YAML"
description: "Parse un fichier de configuration YAML en dictionnaire Python."
category: "2. Load"
tags: ["load", "yaml", "config", "dict"]
code_template: |
  import time
  import os

  # [PARAM: FILE_PATH | file]
  FILE_PATH = "./data/raw/config.yaml"
  # [/PARAM]

  print(f"[LOAD] Lecture du fichier YAML : {FILE_PATH}...")
  if not os.path.exists(FILE_PATH):
      raise FileNotFoundError(f"[ERREUR LOAD] Fichier YAML introuvable : {FILE_PATH}")

  try:
      start_time = time.time()
      import yaml
      
      with open(FILE_PATH, 'r', encoding='utf-8') as f:
          data = yaml.safe_load(f)
          
      duration = time.time() - start_time
      print(f"[LOAD] Succès YAML. Dictionnaire chargé en {duration:.2f}s.")
  except ImportError:
      print("[ERREUR LOAD] Le module 'PyYAML' est requis.")
      raise
  except Exception as e:
      print(f"[ERREUR LOAD] Erreur de parsing YAML : {str(e)}")
      raise e

  df = None
  target_name = None
  task_type = "unknown"
  meta = {"format": "yaml", "keys": len(data) if isinstance(data, dict) else 0}
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_load_yaml" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_load_yaml" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_load_yaml)" stroke="#3b82f6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#3b82f6" opacity="0.15" filter="url(#glow_load_yaml)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#60a5fa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M10 30 L50 70 L90 30" />  </g>  <path d="M 120 50 L 550 50" stroke="#3b82f6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#3b82f6"/>  <circle cx="350" cy="50" r="4" fill="#3b82f6"/>  <circle cx="500" cy="50" r="4" fill="#3b82f6"/></svg>
_metadata:
  bloc: 1
  section: "2_load"
