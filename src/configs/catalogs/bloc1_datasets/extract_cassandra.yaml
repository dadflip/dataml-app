id: extract_cassandra
name: 'Extract: Apache Cassandra'
description: Exécute une requête CQL sur un cluster Cassandra.
when_to_use: À utiliser pour récupérer des données à partir de bases NoSQL orientées colonnes avec de forts volumes de lecture/écriture.
use_cases:
- Extraction d'historiques d'IoT ou de séries temporelles stockés dans Cassandra.
- Récupération de profils utilisateurs à partir d'un cluster distribué.
pros:
- Accès natif aux clusters Cassandra via le langage de requête CQL.
- Conversion automatique des résultats en DataFrame Pandas.
cons:
- Des requêtes non optimisées (sans clé de partition) peuvent être très lentes.
- Gestion des connexions au cluster parfois complexe (timeouts).
category: 1. Extract
tags:
- extract
- cassandra
- cql
- nosql
code_template: |
  from cassandra.cluster import Cluster
  import pandas as pd
  import os
  import time
  
  # [PARAM: HOST | string]
  HOST = "127.0.0.1"
  # [/PARAM]
  
  # [PARAM: KEYSPACE | string]
  KEYSPACE = "my_keyspace"
  # [/PARAM]
  
  # [PARAM: CQL_QUERY | string]
  CQL_QUERY = "SELECT * FROM my_table LIMIT 1000"
  # [/PARAM]
  
  # [PARAM: DEST_PATH | string]
  DEST_PATH = "./data/raw/cassandra_export.csv"
  # [/PARAM]
  
  print(f"[EXTRACT] Connexion au cluster Cassandra ({HOST})...")
  os.makedirs(os.path.dirname(DEST_PATH), exist_ok=True)
  
  try:
      start_time = time.time()
      cluster = Cluster([HOST])
      session = cluster.connect(KEYSPACE)
      
      # Récupération sous forme de dictionnaire dict_factory pour pandas
      from cassandra.query import dict_factory
      session.row_factory = dict_factory
      
      rows = session.execute(CQL_QUERY)
      df_temp = pd.DataFrame(list(rows))
      
      df_temp.to_csv(DEST_PATH, index=False)
      cluster.shutdown()
      
      duration = time.time() - start_time
      print(f"[EXTRACT] Succès Cassandra. {df_temp.shape[0]} lignes exportées en {duration:.2f}s.")
  except Exception as e:
      print(f"[ERREUR EXTRACT] Échec Cassandra : {str(e)}")
      raise e
  
  df = None
  target_name = None
  task_type = "unknown"
  meta = {"source": "cassandra", "path": DEST_PATH}
illustration_mermaid: |
    flowchart LR
        A[/"📄 Cassandra CQL"/] --> B{"Load/Extract Function()"}
        C[/"🎯 TARGET_COL"/] --> D{"Check TARGET_COL"}
        B --> E(["📊 df (DataFrame)"])
        E --> D
        D --> F(["🎯 target_name"])
        D --> G(["⚙️ task_type"])
        D --> H(["ℹ️ meta (...)"])
        style A fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
        style C fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
        style B fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
        style D fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
        style E fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
        style F fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
        style G fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
        style H fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff

_metadata:
  bloc: 1
  section: 1_extract
