id: extract_scraper_bs4
name: 'Extract: Web Scraping (BeautifulSoup)'
description: Extrait le code HTML brut d'une page statique via BeautifulSoup.
when_to_use: Pour récupérer des données textuelles sur des sites web traditionnels sans exécution JavaScript.
use_cases:
  - Scraping d'articles de blog ou de documentation technique.
  - Collecte de données publiques sur des sites à structure HTML simple.
pros:
  - Extrêmement rapide et léger (requêtes HTTP simples).
  - Très populaire avec une vaste documentation disponible.
cons:
  - Incapable de lire le contenu généré dynamiquement via JavaScript.
  - Sensible aux changements de structure HTML (balises, classes).
category: 1. Extract
tags:
- extract
- scraping
- html
- bs4
code_template: |
  import requests
  from bs4 import BeautifulSoup
  import os
  import time

  # [PARAM: TARGET_URL | string]
  TARGET_URL = "https://example.com"
  # [/PARAM]

  # [PARAM: DEST_PATH | string]
  DEST_PATH = "./data/raw/scraped_page.html"
  # [/PARAM]

  print(f"[EXTRACT] Web Scraping de la page {TARGET_URL}...")
  os.makedirs(os.path.dirname(DEST_PATH), exist_ok=True)

  try:
      start_time = time.time()
      headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)'}
      response = requests.get(TARGET_URL, headers=headers, timeout=15)
      response.raise_for_status()
      
      soup = BeautifulSoup(response.content, 'html.parser')
      # On sauvegarde le HTML formatté (prettify)
      with open(DEST_PATH, "w", encoding="utf-8") as f:
          f.write(soup.prettify())
          
      duration = time.time() - start_time
      print(f"[EXTRACT] Succès Scraping. Page sauvegardée en {duration:.2f}s.")
  except requests.exceptions.RequestException as e:
      print(f"[ERREUR EXTRACT] Échec du téléchargement de la page : {str(e)}")
      raise e

  df = DEST_PATH
  target_name = "target"
  task_type = "unknown"
  meta = {"source": "scraping_bs4", "path": DEST_PATH}
illustration_mermaid: |
  flowchart LR
      A[/"📄 TARGET_URL"/] --> B{"requests.get() + bs4"}
      C[/"🎯 TARGET_COL"/] --> D{"Check TARGET_COL"}
      B --> E(["📊 df (HTML Path)"])
      E --> D
      D --> F(["🎯 target_name"])
      D --> G(["⚙️ task_type"])
      D --> H(["ℹ️ meta (bs4)"])
      style A fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
      style C fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
      style B fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style D fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style E fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
      style F fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style G fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style H fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff

_metadata:
  bloc: 1
  section: 1_extract
