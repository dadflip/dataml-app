id: load_gexf
name: 'Load: Fichier GEXF (Gephi)'
description: Charge un graphe au format GEXF (Graph Exchange XML Format) via NetworkX. Exporte les attributs des noeuds dans df et l'edge_index dans meta.
category: 2. Load
tags:
- load
- graph
- gexf
- gephi
- networkx
code_template: |
  import pandas as pd
  import numpy as np
  import time
  try:
      import networkx as nx
  except ImportError:
      nx = None
      print("[ERREUR LOAD] 'networkx' n'est pas installé. Lancez : !pip install networkx")

  # [PARAM: FILE_PATH | file]
  FILE_PATH = "./data/raw/graph.gexf"
  # [/PARAM]

  # [PARAM: TARGET_ATTR | string]
  TARGET_ATTR = ""
  # [/PARAM]

  print(f"[LOAD] Chargement du graphe GEXF depuis {FILE_PATH}...")

  df = pd.DataFrame()
  target_name = ""
  task_type = "unknown"
  meta = {}

  try:
      if nx is not None:
          start_time = time.time()
          G = nx.read_gexf(FILE_PATH)
          
          # Extraction des attributs de noeuds en DataFrame
          node_data = []
          for node_id, attrs in G.nodes(data=True):
              row = {"node_id": node_id}
              row.update(attrs)
              node_data.append(row)
          df = pd.DataFrame(node_data)
          
          # Construction de l'edge_index (format COO : 2 x N_edges)
          node_list = list(G.nodes())
          node2idx = {n: i for i, n in enumerate(node_list)}
          src = [node2idx[u] for u, v in G.edges()]
          dst = [node2idx[v] for u, v in G.edges()]
          edge_index = np.array([src, dst], dtype=np.int64)
          
          duration = time.time() - start_time
          print(f"[LOAD] Succès GEXF. {G.number_of_nodes()} noeuds, {G.number_of_edges()} arêtes en {duration:.2f}s.")
          
          target_name = TARGET_ATTR if TARGET_ATTR and TARGET_ATTR in df.columns else ""
          task_type = "node_classification" if target_name else "graph_unsupervised"
          meta = {"format": "gexf", "shape": df.shape, "edge_index": edge_index, "node_list": node_list}
  except Exception as e:
      print(f"[ERREUR LOAD] Échec du chargement GEXF : {str(e)}")
      raise e
illustration_mermaid: |
  flowchart LR
      A[/"📄 FILE_PATH (.gexf)"/] --> B{"nx.read_gexf()"}
      C[/"🎯 TARGET_ATTR"/] --> D{"Check TARGET_ATTR"}
      B --> E(["📊 df (Node Data)"])
      E --> D
      D --> F(["🎯 target_name"])
      D --> G(["⚙️ task_type"])
      D --> H(["ℹ️ meta (edge_index)"])
      style A fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
      style C fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
      style B fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style D fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style E fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
      style F fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style G fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style H fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff

_metadata:
  bloc: 1
  section: 2_load
