id: load_fastq
name: 'Load: Séquençage FASTQ'
description: Charge un fichier FASTQ (Séquences ADN + Qualité) via Biopython.
category: 2. Load
tags:
- load
- fastq
- bioinformatics
- dna
code_template: "import time\nimport os\n\n# [PARAM: FILE_PATH | file]\nFILE_PATH =\
  \ \"./data/raw/reads.fastq\"\n# [/PARAM]\n\nprint(f\"[LOAD] Parsing FASTQ : {FILE_PATH}...\"\
  )\nif not os.path.exists(FILE_PATH):\n    raise FileNotFoundError(f\"[ERREUR LOAD]\
  \ Fichier FASTQ introuvable : {FILE_PATH}\")\n\ntry:\n    start_time = time.time()\n\
  \    from Bio import SeqIO\n    \n    # Lecture sous forme de générateur pour éviter\
  \ l'OOM sur les très gros fichiers\n    # Ici on charge en mémoire pour l'exemple\n\
  \    records = list(SeqIO.parse(FILE_PATH, \"fastq\"))\n    count = len(records)\n\
  \    \n    duration = time.time() - start_time\n    print(f\"[LOAD] Succès FASTQ.\
  \ {count} reads chargés en {duration:.2f}s.\")\nexcept ImportError:\n    print(\"\
  [ERREUR LOAD] Le module 'biopython' est requis.\")\n    raise\nexcept Exception\
  \ as e:\n    print(f\"[ERREUR LOAD] Erreur de parsing FASTQ : {str(e)}\")\n    raise\
  \ e\n\ndf = None\ntarget_name = None\ntask_type = \"nlp_sequence\"\nmeta = {\"format\"\
  : \"fastq\", \"num_reads\": count}\n"
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_load_fastq" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_load_fastq" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_load_fastq)" stroke="#3b82f6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#3b82f6" opacity="0.15" filter="url(#glow_load_fastq)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#60a5fa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M10 30 L50 70 L90 30" />  </g>  <path d="M 120 50 L 550 50" stroke="#3b82f6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#3b82f6"/>  <circle cx="350" cy="50" r="4" fill="#3b82f6"/>  <circle cx="500" cy="50" r="4" fill="#3b82f6"/></svg>
  width="400" height="200" fill="#0f172a"/><rect x="120" y="60" width="160" height="80"
  fill="#1e293b" stroke="#3b82f6" stroke-width="2"/><text x="140" y="85" fill="#10b981"
  font-size="12" font-family="monospace">@SEQ_ID</text><text x="140" y="105" fill="white"
  font-size="12" font-family="monospace">GATTTGGGGTT...</text><text x="140" y="125"
  fill="#ef4444" font-size="12" font-family="monospace">!*((((***...</text></svg>
_metadata:
  bloc: 1
  section: 2_load
