id: eda_text_length_dist
name: 'EDA: Longueur Textes'
description: Histogramme de la taille des documents textuels.
category: 3. Exploratory Data Analysis
tags:
- eda
- text
- nlp
- seaborn
code_template: |
  import pandas as pd
  import matplotlib.pyplot as plt
  import seaborn as sns
  
  # [PARAM: TEXT_COL | string]
  TEXT_COL = "text"
  # [/PARAM]
  
  if TEXT_COL in df.columns:
      lengths = df[TEXT_COL].apply(lambda x: len(str(x).split()))
      plt.figure(figsize=(10, 5))
      sns.histplot(lengths, bins=50, kde=True)
      plt.title(f"Distribution de la longueur (mots) - {TEXT_COL}")
      plt.xlabel("Nombre de mots")
      plt.show()
      print(f"[EDA] Longueur max : {lengths.max()}, Moyenne : {lengths.mean():.1f}")
  else:
      print(f"[EDA] Colonne {TEXT_COL} introuvable.")
illustration_mermaid: |
    graph TD
      DF([DataFrame]) --> Check{Check TEXT_COL}
      Check -- Valid --> Calc[Calculate word counts]
      Calc --> Hist[sns.histplot]
      Hist --> Show[plt.show & Print stats]
      Check -- Missing --> Err[Print Error]
      style Calc fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style Hist fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
_metadata:
  bloc: 1
  section: 3_eda
