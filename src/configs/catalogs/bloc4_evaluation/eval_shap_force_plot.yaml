id: "eval_shap_force_plot"
name: "Eval Plot: SHAP Force Plot (Interactive HTML)"
description: "Visualisation SHAP classique des forces agissant sur une prédiction, générée sous forme de page HTML interactive."
category: "Explainability & Feature Importance"
tags: ["eval", "plot", "explainability", "shap", "html", "force-plot"]
applies_to: ["Classification", "Regression"]
when_to_use: "Idéal pour intégrer une explication interactive dans un dashboard web."
pros:
  - "Interactif."
cons:
  - "Nécessite de charger un fichier HTML séparé."
code_template: |
  import os
  import numpy as np
  try:
      import shap
  except ImportError:
      shap = None

  evaluation_metrics = {}

  if 'full_pipeline' in locals() and 'X_test' in locals() and shap is not None:
      print("[EVAL] Génération du SHAP Force Plot HTML...")
      try:
          os.makedirs("outputs/evaluation", exist_ok=True)
          
          X_arr = np.array(X_test)
          instance = X_arr[0]
          background = X_arr[0:50] if len(X_arr) > 50 else X_arr
              
          model = full_pipeline.steps[-1][1] if hasattr(full_pipeline, 'steps') else full_pipeline
          
          explainer = shap.Explainer(model.predict, background)
          shap_values = explainer(instance.reshape(1, -1))
          
          # Initialize JS pour l'export HTML
          shap.initjs()
          
          force_plot = shap.plots.force(shap_values.base_values[0], shap_values.values[0], instance)
          
          html_path = "outputs/evaluation/shap_force_plot.html"
          shap.save_html(html_path, force_plot)
          
          evaluation_metrics["shap_force_plot_html"] = html_path
          print(f"[EVAL] Fichier HTML sauvegardé sous {html_path}")
      except Exception as e:
          print(f"[EVAL ERREUR] SHAP Force Plot : {str(e)}")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_shap_force_plot" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_shap_force_plot" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_shap_force_plot)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_shap_force_plot)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
