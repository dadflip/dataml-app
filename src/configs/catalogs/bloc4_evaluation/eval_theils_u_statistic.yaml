id: "eval_theils_u_statistic"
name: "Eval Metric: Theil's U Statistic"
description: "Mesure de la précision des prévisions financières ou macro-économiques. Borne basse = 0 (parfait), 1 = aussi bon qu'un modèle naïf."
category: "Time Series Metrics"
tags: ["eval", "metric", "timeseries", "forecasting", "finance"]
applies_to: ["Forecasting"]
when_to_use: "Standards de facto en économétrie et en finance pour comparer des modèles de prévision."
pros:
  - "Très interprétable par les économistes."
cons:
  - "Sensible aux valeurs aberrantes de par la mise au carré."
code_template: |
  import numpy as np

  evaluation_metrics = {}

  if task_type in ["forecasting", "regression"] and 'y_test' in locals() and 'y_pred' in locals():
      print("[EVAL] Calcul du Theil's U Statistic...")
      try:
          actual = np.array(y_test)
          forecast = np.array(y_pred)
          
          if len(actual) > 1:
              # Différences pour le modèle Naïf
              diff_actual = np.diff(actual)
              diff_forecast = forecast[1:] - actual[:-1]
              
              num = np.sqrt(np.mean((diff_actual - diff_forecast) ** 2))
              den = np.sqrt(np.mean(diff_actual ** 2))
              
              if den > 0:
                  theils_u = num / den
                  evaluation_metrics["theils_u"] = float(theils_u)
                  print(f"[EVAL] Theil's U1: {theils_u:.4f} (Doit être < 1 pour être meilleur que Naïf)")
              else:
                  print("[EVAL] Dénominateur Theil's U nul.")
          else:
              print("[EVAL] Pas assez de données.")
      except Exception as e:
          print(f"[EVAL ERREUR] Theil's U : {str(e)}")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_theils_u_statistic" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_theils_u_statistic" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_theils_u_statistic)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_theils_u_statistic)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
