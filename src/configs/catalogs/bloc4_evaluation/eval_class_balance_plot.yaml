id: "eval_class_balance_plot"
name: "Eval Plot: Class Balance Comparison"
description: "Compare sous forme d'histogramme la distribution des classes réelles par rapport aux classes prédites."
category: "Classification Visualizations"
tags: ["eval", "plot", "classification", "class-balance"]
applies_to: ["Classification"]
when_to_use: "Pour voir en un clin d'oeil si le modèle sous-prédit ou sur-prédit une classe spécifique."
pros:
  - "Très simple et intuitif."
cons:
  - "Ne remplace pas la matrice de confusion (qui montre QUI se trompe avec QUI)."
code_template: |
  import os
  import numpy as np
  import matplotlib.pyplot as plt

  evaluation_metrics = {}

  if task_type == "classification" and 'y_test' in locals() and 'y_pred' in locals():
      print("[EVAL] Génération du Class Balance Plot...")
      try:
          os.makedirs("outputs/evaluation", exist_ok=True)
          
          # Comptage
          classes, counts_true = np.unique(y_test, return_counts=True)
          pred_classes, counts_pred = np.unique(y_pred, return_counts=True)
          
          # Mapping pour s'assurer que l'ordre est le même
          dict_pred = dict(zip(pred_classes, counts_pred))
          aligned_counts_pred = [dict_pred.get(c, 0) for c in classes]
          
          x = np.arange(len(classes))
          width = 0.35
          
          fig, ax = plt.subplots(figsize=(10, 6))
          rects1 = ax.bar(x - width/2, counts_true, width, label='Réel (y_test)', color='skyblue')
          rects2 = ax.bar(x + width/2, aligned_counts_pred, width, label='Prédit (y_pred)', color='salmon')
          
          ax.set_ylabel('Nombre d\'instances')
          ax.set_title('Distribution des classes : Réel vs Prédit')
          ax.set_xticks(x)
          ax.set_xticklabels([str(c) for c in classes], rotation=45 if len(classes)>5 else 0)
          ax.legend()
          ax.grid(axis='y', alpha=0.3)
          
          plt.tight_layout()
          
          plot_path = "outputs/evaluation/class_balance_plot.png"
          fig.savefig(plot_path, dpi=300)
          plt.close(fig)
          
          evaluation_metrics["class_balance_plot"] = plot_path
          print(f"[EVAL] Plot sauvegardé sous {plot_path}")
      except Exception as e:
          print(f"[EVAL ERREUR] : {str(e)}")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_class_balance_plot" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_class_balance_plot" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_class_balance_plot)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_class_balance_plot)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
