id: "eval_rmse"
name: "Eval: Root Mean Squared Error (RMSE)"
description: >
  Calcule la racine de l'erreur quadratique moyenne (RMSE) entre les valeurs reelles
  et les predictions. Le RMSE est exprime dans la meme unite que la variable cible,
  ce qui le rend plus interpretable que le MSE.
category: "Metriques de Regression"
tags: ["eval", "regression", "rmse", "erreur", "metrique"]
applies_to: ["Regression"]
when_to_use: >
  Utiliser lorsque l'on souhaite une mesure d'erreur interpretable dans la meme unite
  que la variable cible, tout en penalisant les grandes erreurs.
pros:
  - "Meme unite que la variable cible, donc facilement interpretable"
  - "Penalise les grandes erreurs plus que le MAE"
  - "Metrique standard pour les competitions et benchmarks"
cons:
  - "Sensible aux valeurs aberrantes (outliers)"
  - "Ne permet pas de comparer des modeles sur des echelles differentes"
  - "Moins robuste que le MAE face aux distributions asymetriques"
code_template: |
  evaluation_metrics = {}
  try:
      from sklearn.metrics import mean_squared_error
      import numpy as np

      print("[EVAL] Calcul du Root Mean Squared Error (RMSE)...")

      mse_value = mean_squared_error(y_test, y_pred)
      rmse_value = np.sqrt(mse_value)

      evaluation_metrics["rmse"] = float(rmse_value)
      evaluation_metrics["mse"] = float(mse_value)

      print(f"[EVAL] RMSE = {rmse_value:.6f}")

  except Exception as e:
      print(f"[EVAL] Erreur lors du calcul du RMSE : {e}")
      evaluation_metrics["rmse"] = None
      evaluation_metrics["error"] = str(e)
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_rmse" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_rmse" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_rmse)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_rmse)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
