id: "eval_meteor_score"
name: "Eval Metric: METEOR Score"
description: "Calcule le score METEOR (Metric for Evaluation of Translation with Explicit ORdering) pour la génération de texte."
category: "NLP Metrics"
tags: ["eval", "metric", "nlp", "generation", "meteor"]
applies_to: ["NLP Generation"]
when_to_use: "Pour évaluer la traduction automatique ou le résumé avec une meilleure corrélation avec le jugement humain que BLEU."
pros:
  - "Prend en compte la synonymie et la racinisation."
cons:
  - "Lent à calculer, dépend de NLTK."
code_template: |
  import os
  try:
      import nltk
      from nltk.translate.meteor_score import meteor_score
  except ImportError:
      nltk = None

  evaluation_metrics = {}

  if 'y_test' in locals() and 'y_pred' in locals() and nltk is not None:
      print("[EVAL] Calcul du score METEOR...")
      try:
          nltk.download('wordnet', quiet=True)
          
          if isinstance(y_test[0], str) and isinstance(y_pred[0], str):
              scores = []
              for ref, pred in zip(y_test, y_pred):
                  # meteor_score prend une liste de références, et une prédiction tokenize
                  scores.append(meteor_score([ref.split()], pred.split()))
                  
              mean_meteor = sum(scores) / len(scores)
              evaluation_metrics["meteor_score"] = float(mean_meteor)
              
              print(f"[EVAL] METEOR Score: {mean_meteor:.3f}")
          else:
              print("[EVAL] y_test/y_pred ne sont pas des chaînes de caractères.")
      except Exception as e:
          print(f"[EVAL ERREUR] METEOR : {str(e)}")
  elif nltk is None:
      print("[EVAL ERREUR] Le package 'nltk' n'est pas installé.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_meteor_score" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_meteor_score" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_meteor_score)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_meteor_score)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
