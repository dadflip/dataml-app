id: "eval_class_prediction_error"
name: "Eval Plot: Class Prediction Error"
description: "Visualisation sous forme de barres empilées montrant les erreurs de prédiction par classe (inspiré de Yellowbrick)."
category: "Classification Visualizations"
tags: ["eval", "plot", "classification", "matplotlib", "error-analysis"]
applies_to: ["Classification"]
when_to_use: "Pour visualiser rapidement quelles classes sont confondues avec quelles autres de manière proportionnelle."
pros:
  - "Plus lisible qu'une matrice de confusion brute pour les jeux de données déséquilibrés."
cons:
  - "Devient illisible s'il y a trop de classes."
code_template: |
  import os
  import numpy as np
  import pandas as pd
  import matplotlib.pyplot as plt
  from sklearn.metrics import confusion_matrix

  evaluation_metrics = {}

  if task_type == "classification" and 'y_test' in locals() and 'y_pred' in locals():
      print("[EVAL] Génération du Class Prediction Error Plot...")
      try:
          os.makedirs("outputs/evaluation", exist_ok=True)
          cm = confusion_matrix(y_test, y_pred)
          classes = np.unique(np.concatenate([y_test, y_pred]))
          
          fig, ax = plt.subplots(figsize=(10, 6))
          
          # Barres empilées
          bottom = np.zeros(len(classes))
          colors = plt.cm.get_cmap('tab20', len(classes))
          
          for i, cls_pred in enumerate(classes):
              ax.bar(classes, cm[:, i], bottom=bottom, label=f'Pred: {cls_pred}', color=colors(i))
              bottom += cm[:, i]
              
          ax.set_xlabel('Classe Réelle')
          ax.set_ylabel('Nombre de Prédictions')
          ax.set_title('Class Prediction Error')
          ax.legend(title='Classe Prédite', bbox_to_anchor=(1.05, 1), loc='upper left')
          plt.tight_layout()
          
          plot_path = "outputs/evaluation/class_prediction_error.png"
          fig.savefig(plot_path, dpi=300)
          plt.close(fig)
          
          evaluation_metrics["class_prediction_error_plot"] = plot_path
          print(f"[EVAL] Plot sauvegardé sous {plot_path}")
      except Exception as e:
          print(f"[EVAL ERREUR] Impossible de générer le plot : {str(e)}")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_class_prediction_error" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_class_prediction_error" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_class_prediction_error)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_class_prediction_error)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
