id: "eval_feature_distribution_by_class"
name: "Eval Plot: Feature Distribution by Class"
description: "Affiche la distribution d'une feature clé, séparée par les classes prédites, pour voir comment la feature discrimine la sortie."
category: "Explainability & Feature Importance"
tags: ["eval", "plot", "explainability", "distribution", "kde"]
applies_to: ["Classification"]
when_to_use: "Pour vérifier visuellement si une feature spécifique est un bon discriminant pour le modèle."
pros:
  - "Simple, lisible, ne nécessite pas de SHAP/LIME."
cons:
  - "Uniquement univarié."
code_template: |
  import os
  import numpy as np
  import matplotlib.pyplot as plt
  try:
      import seaborn as sns
  except ImportError:
      sns = None

  evaluation_metrics = {}

  if task_type == "classification" and 'X_test' in locals() and 'y_pred' in locals():
      print("[EVAL] Génération de la Distribution de Feature par Classe...")
      try:
          os.makedirs("outputs/evaluation", exist_ok=True)
          
          X_arr = np.array(X_test)
          if len(X_arr.shape) > 2:
              X_arr = X_arr.reshape(X_arr.shape[0], -1)
              
          # On prend la première feature arbitrairement (idéalement celle avec le plus d'importance)
          feat_idx = 0
          feature_data = X_arr[:, feat_idx]
          
          feat_name = "Feature 0"
          if 'feature_names_out' in locals() and len(feature_names_out) > feat_idx:
              feat_name = feature_names_out[feat_idx]
              
          classes = np.unique(y_pred)
          
          fig, ax = plt.subplots(figsize=(10, 6))
          
          if sns is not None:
              for c in classes:
                  sns.kdeplot(feature_data[y_pred == c], label=f'Class {c}', fill=True, ax=ax)
          else:
              for c in classes:
                  ax.hist(feature_data[y_pred == c], bins=30, alpha=0.5, density=True, label=f'Class {c}')
                  
          ax.set_title(f'Distribution de {feat_name} selon les prédictions')
          ax.set_xlabel(feat_name)
          ax.set_ylabel('Densité')
          ax.legend()
          
          plot_path = f"outputs/evaluation/feature_dist_{feat_idx}.png"
          fig.savefig(plot_path, dpi=300)
          plt.close(fig)
          
          evaluation_metrics["feature_dist_plot"] = plot_path
          print(f"[EVAL] Plot sauvegardé sous {plot_path}")
      except Exception as e:
          print(f"[EVAL ERREUR] Feature Dist : {str(e)}")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_feature_distribution_by_class" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_feature_distribution_by_class" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_feature_distribution_by_class)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_feature_distribution_by_class)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
