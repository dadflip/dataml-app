id: "eval_shap_dependence_plot"
name: "Eval Plot: SHAP Dependence"
description: "Montre comment la valeur d'une feature affecte la sortie SHAP du modèle, en colorant par une seconde feature pour révéler les interactions."
category: "Explainability & Feature Importance"
tags: ["eval", "plot", "explainability", "shap", "dependence"]
applies_to: ["Classification", "Regression"]
when_to_use: "Pour comprendre l'effet global d'une feature et découvrir ses interactions avec d'autres."
pros:
  - "Très rigoureux mathématiquement (valeurs de Shapley)."
cons:
  - "Extrêmement lent sur des réseaux de neurones ou pipelines complexes (nécessite le KernelExplainer ou TreeExplainer)."
code_template: |
  import os
  import numpy as np
  import matplotlib.pyplot as plt
  try:
      import shap
  except ImportError:
      shap = None

  evaluation_metrics = {}

  if 'full_pipeline' in locals() and 'X_test' in locals() and shap is not None:
      print("[EVAL] Génération du SHAP Dependence Plot...")
      try:
          os.makedirs("outputs/evaluation", exist_ok=True)
          
          X_arr = np.array(X_test)
          if len(X_arr) > 200:
              idx = np.random.choice(len(X_arr), 200, replace=False)
              X_sample = X_arr[idx]
          else:
              X_sample = X_arr
              
          model = full_pipeline.steps[-1][1] if hasattr(full_pipeline, 'steps') else full_pipeline
          
          # Tentative d'utilisation de l'explainer générique
          explainer = shap.Explainer(model.predict, X_sample)
          shap_values = explainer(X_sample)
          
          fig, ax = plt.subplots(figsize=(8, 6))
          # Plot sur la première feature
          shap.plots.scatter(shap_values[:, 0], color=shap_values, show=False)
          
          plot_path = "outputs/evaluation/shap_dependence.png"
          fig.savefig(plot_path, dpi=300, bbox_inches='tight')
          plt.close(fig)
          
          evaluation_metrics["shap_dependence_plot"] = plot_path
          print(f"[EVAL] Plot sauvegardé sous {plot_path}")
      except Exception as e:
          print(f"[EVAL ERREUR] SHAP Dependence : {str(e)}")
  elif shap is None:
      print("[EVAL ERREUR] SHAP n'est pas installé. Lancez : pip install shap")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_shap_dependence_plot" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_shap_dependence_plot" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_shap_dependence_plot)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_shap_dependence_plot)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
