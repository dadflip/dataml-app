id: "eval_actual_vs_predicted_kde"
name: "Eval Plot: Actual vs Predicted KDE Distributions"
description: "Compare la distribution (Kernel Density Estimate) des vraies valeurs par rapport aux prédictions."
category: "Regression Visualizations"
tags: ["eval", "plot", "regression", "distribution", "kde", "seaborn"]
applies_to: ["Regression"]
when_to_use: "Pour vérifier si le modèle arrive à reproduire la distribution globale (moments statistiques) de la variable cible."
pros:
  - "Montre bien si le modèle 'écrase' les valeurs extrêmes."
cons:
  - "Nécessite seaborn pour une belle visualisation."
code_template: |
  import os
  import numpy as np
  import matplotlib.pyplot as plt
  try:
      import seaborn as sns
  except ImportError:
      sns = None

  evaluation_metrics = {}

  if task_type == "regression" and 'y_test' in locals() and 'y_pred' in locals():
      print("[EVAL] Génération du Actual vs Predicted KDE Plot...")
      try:
          os.makedirs("outputs/evaluation", exist_ok=True)
          
          fig, ax = plt.subplots(figsize=(10, 6))
          
          if sns is not None:
              sns.kdeplot(y_test, color="blue", label="Valeurs Réelles", fill=True, ax=ax)
              sns.kdeplot(y_pred, color="red", label="Valeurs Prédites", fill=True, alpha=0.6, ax=ax)
          else:
              # Fallback basique Matplotlib histogram
              ax.hist(y_test, bins=50, alpha=0.5, color='blue', density=True, label="Valeurs Réelles")
              ax.hist(y_pred, bins=50, alpha=0.5, color='red', density=True, label="Valeurs Prédites")
              
          ax.set_title('Distribution de Densité : Réel vs Prédit')
          ax.set_xlabel('Valeur Cible')
          ax.set_ylabel('Densité')
          ax.legend()
          
          plot_path = "outputs/evaluation/actual_vs_predicted_kde.png"
          fig.savefig(plot_path, dpi=300)
          plt.close(fig)
          
          evaluation_metrics["actual_vs_predicted_kde_plot"] = plot_path
          print(f"[EVAL] Plot sauvegardé sous {plot_path}")
      except Exception as e:
          print(f"[EVAL ERREUR] : {str(e)}")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_actual_vs_predicted_kde" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_actual_vs_predicted_kde" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_actual_vs_predicted_kde)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_actual_vs_predicted_kde)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
