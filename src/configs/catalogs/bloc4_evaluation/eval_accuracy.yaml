id: "eval_accuracy"
name: "Eval: Accuracy Score"
description: "Calcule le taux de bonnes predictions (accuracy). Proportion d'echantillons correctement classes parmi l'ensemble des predictions."
category: "Metriques de Classification"
tags: ["eval", "classification", "accuracy", "sklearn"]
applies_to: ["Classification"]
when_to_use: "Metrique de base pour evaluer un classifieur, surtout lorsque les classes sont equilibrees."
pros:
  - "Intuitive et facile a interpreter."
  - "Fonctionne pour les problemes binaires et multi-classes."
cons:
  - "Trompeuse sur des jeux de donnees desequilibres (une classe majoritaire gonfle le score)."
  - "Ne distingue pas les types d'erreurs (faux positifs vs faux negatifs)."
code_template: |
  import numpy as np
  from sklearn.metrics import accuracy_score, balanced_accuracy_score

  evaluation_metrics = {}

  print("[EVAL] Calcul de l'Accuracy Score...")

  try:
      acc = accuracy_score(y_test, y_pred)
      evaluation_metrics["accuracy"] = float(np.round(acc, 6))
      print(f"[EVAL] Accuracy : {acc:.4f}")

      bal_acc = balanced_accuracy_score(y_test, y_pred)
      evaluation_metrics["balanced_accuracy"] = float(np.round(bal_acc, 6))
      print(f"[EVAL] Balanced Accuracy : {bal_acc:.4f}")

      n_correct = int(np.sum(np.array(y_test) == np.array(y_pred)))
      n_total = len(y_test)
      evaluation_metrics["n_correct"] = n_correct
      evaluation_metrics["n_total"] = n_total
      print(f"[EVAL] {n_correct} / {n_total} predictions correctes.")

  except Exception as e:
      print(f"[EVAL][ERREUR] Echec du calcul de l'accuracy : {e}")
      evaluation_metrics["error"] = str(e)
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eval_accuracy" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eval_accuracy" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eval_accuracy)" stroke="#f59e0b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#f59e0b" opacity="0.15" filter="url(#glow_eval_accuracy)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#fbbf24" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L50 30 L80 60 L120 20" />  </g>  <path d="M 120 50 L 550 50" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#f59e0b"/>  <circle cx="350" cy="50" r="4" fill="#f59e0b"/>  <circle cx="500" cy="50" r="4" fill="#f59e0b"/></svg>
_metadata:
  bloc: 4
  section: "1_evaluation"
