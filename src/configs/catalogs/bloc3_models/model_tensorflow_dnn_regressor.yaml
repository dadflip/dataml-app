id: "model_tensorflow_dnn_regressor"
name: "Model: TensorFlow/Keras DNN (Regression)"
description: |
  Réseau de neurones profond (Deep Neural Network) pour les tâches de régression, implémenté avec l'API Keras de TensorFlow. Il génère une prédiction continue au travers de couches denses (Multi-Layer Perceptron).
category: "Réseaux de Neurones"
tags: ["model", "regression", "tabular", "tensorflow", "keras", "dnn", "deep-learning"]
applies_to: ["Tabulaire"]
when_to_use: |
  Pour des tâches de régression tabulaire massives (des centaines de milliers de lignes ou plus), nécessitant un modèle hautement scalable capable d'apprendre des relations non linéaires complexes.
use_cases:
  - "Prévision de la demande ou modélisation de séries temporelles avec de nombreuses covariables."
  - "Évaluation immobilière ou tarification d'assurance sur de très vastes jeux de données."
  - "Modélisation de processus physiques ou industriels (création de jumeaux numériques)."
pros:
  - "Très scalable : gestion aisée des gros volumes de données via l'entraînement par batch."
  - "Intégration directe avec l'écosystème de production TensorFlow (Serving, TFX)."
  - "Modulable : la fonction de perte (MSE, MAE, Huber) s'adapte facilement aux valeurs aberrantes."
cons:
  - "Extrêmement sensible à l'échelle (scaling) des variables d'entrée (nécessite une normalisation stricte)."
  - "Performances souvent inférieures aux arbres boostés (XGBoost) sur les petits/moyens datasets."
  - "Effet 'boîte noire' limitant l'interprétabilité globale et locale du modèle."
hyperparameters:
  hidden_layers: "Tailles de couches séparées par des virgules (ex: '256, 128')."
  epochs: "Nombre d'époques d'entraînement (ex: 20)."
math_theory: |
  Le réseau de neurones profond calcule la prédiction continue $\textcolor{#10b981}{\hat{y}}$ en propageant l'entrée $\textcolor{#fbbf24}{x}$ à travers plusieurs couches :
  $$
  \textcolor{#10b981}{\hat{y}} = \textcolor{#8b5cf6}{W}^{\textcolor{#8b5cf6}{(L)}} \textcolor{#3b82f6}{\sigma}(\dots \textcolor{#3b82f6}{\sigma}(\textcolor{#8b5cf6}{W}^{\textcolor{#8b5cf6}{(1)}} \textcolor{#fbbf24}{x} + \textcolor{#8b5cf6}{b}^{\textcolor{#8b5cf6}{(1)}}) \dots ) + \textcolor{#8b5cf6}{b}^{\textcolor{#8b5cf6}{(L)}}
  $$
  L'entraînement est effectué en minimisant une fonction de perte $\textcolor{#ef4444}{\mathcal{L}}$ telle que l'Erreur Quadratique Moyenne (MSE) :
  $$
  \textcolor{#ef4444}{\mathcal{L}} = \frac{1}{N} \sum_{i=1}^{N} (\textcolor{#fbbf24}{y}_{\textcolor{#fbbf24}{i}} - \textcolor{#10b981}{\hat{y}}_{\textcolor{#10b981}{i}})^2
  $$
math_illustration_svg: |
  <svg viewBox="0 0 800 400" xmlns="http://www.w3.org/2000/svg">
    <defs>
      <linearGradient id="bgGrad4" x1="0%" y1="0%" x2="100%" y2="100%">
        <stop offset="0%" stop-color="#121212" />
        <stop offset="100%" stop-color="#1e1e1e" />
      </linearGradient>
      <marker id="arrowPurple4" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#8b5cf6" />
      </marker>
      <marker id="arrowGreen4" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#10b981" />
      </marker>
      <marker id="arrowRed4" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#ef4444" />
      </marker>
    </defs>
    <rect width="100%" height="100%" fill="url(#bgGrad4)" rx="15" />
    <text x="400" y="35" fill="#ffffff" font-family="sans-serif" font-size="22" font-weight="bold" text-anchor="middle">TensorFlow DNN Regressor Architecture</text>
    
    <!-- Input -->
    <rect x="40" y="150" width="80" height="100" fill="#fbbf24" fill-opacity="0.15" stroke="#fbbf24" stroke-width="2" rx="8" />
    <text x="80" y="195" fill="#fbbf24" font-family="sans-serif" font-size="18" font-weight="bold" text-anchor="middle">Input x</text>
    <text x="80" y="215" fill="#fbbf24" font-family="sans-serif" font-size="14" text-anchor="middle">a^(0)</text>

    <!-- Dense L1 -->
    <rect x="180" y="80" width="120" height="240" fill="#3b82f6" fill-opacity="0.1" stroke="#3b82f6" stroke-width="2" rx="8" />
    <text x="240" y="110" fill="#3b82f6" font-family="sans-serif" font-size="16" font-weight="bold" text-anchor="middle">Dense Layer 1</text>
    
    <rect x="200" y="130" width="80" height="40" fill="#8b5cf6" fill-opacity="0.2" stroke="#8b5cf6" stroke-width="1.5" rx="4" />
    <text x="240" y="155" fill="#8b5cf6" font-family="sans-serif" font-size="14" font-weight="bold" text-anchor="middle">W^(1), b^(1)</text>

    <rect x="200" y="180" width="80" height="40" fill="#3b82f6" fill-opacity="0.2" stroke="#3b82f6" stroke-width="1.5" rx="4" />
    <text x="240" y="205" fill="#3b82f6" font-family="sans-serif" font-size="14" font-weight="bold" text-anchor="middle">z^(1)</text>

    <rect x="200" y="230" width="80" height="40" fill="#10b981" fill-opacity="0.2" stroke="#10b981" stroke-width="1.5" rx="4" />
    <text x="240" y="255" fill="#10b981" font-family="sans-serif" font-size="14" font-weight="bold" text-anchor="middle">ReLU (a^(1))</text>
    
    <rect x="200" y="280" width="80" height="25" fill="#ffffff" fill-opacity="0.1" stroke="#ffffff" stroke-width="1" rx="4" />
    <text x="240" y="297" fill="#ffffff" font-family="sans-serif" font-size="11" text-anchor="middle">BatchNorm</text>

    <!-- Dense L2 -->
    <rect x="360" y="100" width="120" height="200" fill="#3b82f6" fill-opacity="0.1" stroke="#3b82f6" stroke-width="2" rx="8" />
    <text x="420" y="130" fill="#3b82f6" font-family="sans-serif" font-size="16" font-weight="bold" text-anchor="middle">Dense Layer 2</text>
    
    <rect x="380" y="150" width="80" height="40" fill="#8b5cf6" fill-opacity="0.2" stroke="#8b5cf6" stroke-width="1.5" rx="4" />
    <text x="420" y="175" fill="#8b5cf6" font-family="sans-serif" font-size="14" font-weight="bold" text-anchor="middle">W^(2), b^(2)</text>

    <rect x="380" y="200" width="80" height="40" fill="#3b82f6" fill-opacity="0.2" stroke="#3b82f6" stroke-width="1.5" rx="4" />
    <text x="420" y="225" fill="#3b82f6" font-family="sans-serif" font-size="14" font-weight="bold" text-anchor="middle">z^(2)</text>

    <rect x="380" y="250" width="80" height="30" fill="#10b981" fill-opacity="0.2" stroke="#10b981" stroke-width="1.5" rx="4" />
    <text x="420" y="270" fill="#10b981" font-family="sans-serif" font-size="12" font-weight="bold" text-anchor="middle">ReLU (a^(2))</text>

    <!-- Output Layer -->
    <rect x="540" y="130" width="100" height="140" fill="#3b82f6" fill-opacity="0.1" stroke="#3b82f6" stroke-width="2" rx="8" />
    <text x="590" y="160" fill="#3b82f6" font-family="sans-serif" font-size="16" font-weight="bold" text-anchor="middle">Output</text>
    
    <rect x="550" y="180" width="80" height="40" fill="#8b5cf6" fill-opacity="0.2" stroke="#8b5cf6" stroke-width="1.5" rx="4" />
    <text x="590" y="205" fill="#8b5cf6" font-family="sans-serif" font-size="14" font-weight="bold" text-anchor="middle">W^(L), b^(L)</text>

    <rect x="550" y="230" width="80" height="30" fill="#10b981" fill-opacity="0.2" stroke="#10b981" stroke-width="1.5" rx="4" />
    <text x="590" y="250" fill="#10b981" font-family="sans-serif" font-size="12" font-weight="bold" text-anchor="middle">Identity</text>

    <!-- Final Output -->
    <circle cx="720" cy="200" r="25" fill="#10b981" fill-opacity="0.2" stroke="#10b981" stroke-width="2" />
    <text x="720" y="205" fill="#10b981" font-family="sans-serif" font-size="18" font-weight="bold" text-anchor="middle">ŷ</text>

    <!-- Connections -->
    <path d="M 120 200 L 170 200" fill="none" stroke="#fbbf24" stroke-width="2" marker-end="url(#arrowPurple4)" />
    <path d="M 300 200 L 350 200" fill="none" stroke="#10b981" stroke-width="2" marker-end="url(#arrowGreen4)" />
    <path d="M 480 200 L 530 200" fill="none" stroke="#10b981" stroke-width="2" marker-end="url(#arrowGreen4)" />
    <path d="M 640 200 L 685 200" fill="none" stroke="#10b981" stroke-width="2" marker-end="url(#arrowGreen4)" />

    <!-- Loss -->
    <rect x="580" y="300" width="180" height="60" fill="#ef4444" fill-opacity="0.15" stroke="#ef4444" stroke-width="2" rx="10" />
    <text x="670" y="325" fill="#ef4444" font-family="sans-serif" font-size="14" font-weight="bold" text-anchor="middle">MSE Loss (L)</text>
    <text x="670" y="345" fill="#ef4444" font-family="sans-serif" font-size="12" text-anchor="middle">1/N Σ(y - ŷ)²</text>
    <path d="M 720 225 L 720 300" fill="none" stroke="#ef4444" stroke-width="1.5" stroke-dasharray="4" marker-end="url(#arrowRed4)" />
  </svg>
code_template: |
  import pandas as pd
  import numpy as np
  try:
      import tensorflow as tf
      from tensorflow import keras
      from tensorflow.keras import layers
  except ImportError:
      tf = None
      print("[ERREUR] tensorflow n'est pas installé. Lancez : !pip install tensorflow")

  # [PARAM: HIDDEN_LAYERS | string]
  HIDDEN_LAYERS = "256, 128"
  # [/PARAM]

  # [PARAM: EPOCHS | number]
  EPOCHS = 20
  # [/PARAM]

  print(f"[MODEL] Entraînement TensorFlow/Keras DNN (Regression)...")
  model = None
  full_pipeline = None
  y_pred = None
  y_proba = None
  feature_importances = None
  training_history = {}
  task_type = "regression"
  meta = {"model_name": "TensorFlow DNN Regressor"}

  if 'X_train' in locals() and 'y_train' in locals() and 'X_test' in locals() and tf is not None:
      X_tr = np.array(X_train, dtype=np.float32)
      X_te = np.array(X_test, dtype=np.float32)
      y_tr = np.array(y_train, dtype=np.float32)
      
      layer_sizes = [int(s.strip()) for s in HIDDEN_LAYERS.split(',') if s.strip().isdigit()]
      if not layer_sizes:
          layer_sizes = [256, 128]
          
      inputs = keras.Input(shape=(X_tr.shape[1],))
      x = inputs
      for size in layer_sizes:
          x = layers.Dense(size, activation='relu')(x)
          x = layers.BatchNormalization()(x)
          x = layers.Dropout(0.2)(x)
          
      outputs = layers.Dense(1)(x)
      
      model = keras.Model(inputs=inputs, outputs=outputs)
      model.compile(optimizer='adam', loss='mse', metrics=['mae'])
      
      history = model.fit(X_tr, y_tr, epochs=int(EPOCHS), batch_size=32, validation_split=0.1, verbose=0)
      
      y_pred = model.predict(X_te, verbose=0).flatten()
      
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          full_pipeline = model
          
      training_history = {
          "status": "success",
          "loss": float(history.history['loss'][-1]),
          "val_loss": float(history.history.get('val_loss', [0.0])[-1])
      }
illustration_mermaid: |
  flowchart LR
    Data(["Données Tabulaires d'Entrée"])
    Params[/"Hyperparamètres (layers, epochs)"/]
    Model{"TensorFlow / Keras DNN (Regressor)"}
    Pred(["Prédictions Continues"])
    
    Data --> Model
    Params --> Model
    Model --> Pred

    style Data fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
    style Params fill:#fbbf24,stroke:#d97706,stroke-width:2px,color:#000
    style Model fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
    style Pred fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff

_metadata:
  bloc: 3
  section: "5_clustering_automl_and_nn"
