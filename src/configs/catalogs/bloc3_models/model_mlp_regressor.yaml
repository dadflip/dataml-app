id: "model_mlp_regressor"
name: "Model: Multi-Layer Perceptron (Regressor)"
description: "Réseau de neurones artificiel standard (fully connected) via Scikit-Learn pour la régression."
category: "Réseaux de Neurones"
tags: ["model", "regression", "tabular", "sklearn", "mlp", "neural-network"]
applies_to: ["Tabulaire"]
when_to_use: "Baseline rapide de deep learning pour des prédictions continues."
pros:
  - "Support natif Sklearn."
cons:
  - "Lent sur les très gros datasets."
hyperparameters:
  hidden_layer_sizes: "Tailles des couches (ex: '100, 50')."
  activation: "Fonction d'activation ('relu', 'tanh', 'logistic')."
  max_iter: "Nombre max d'époques (ex: 200)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.neural_network import MLPRegressor

  # [PARAM: HIDDEN_LAYER_SIZES | string]
  HIDDEN_LAYER_SIZES = "100, 50"
  # [/PARAM]

  # [PARAM: ACTIVATION | string]
  ACTIVATION = "relu"
  # [/PARAM]

  # [PARAM: MAX_ITER | number]
  MAX_ITER = 200
  # [/PARAM]

  print(f"[MODEL] Entraînement MLP Regressor (layers={HIDDEN_LAYER_SIZES})...")
  if 'X_train' in locals() and 'y_train' in locals() and 'X_test' in locals():
      layers = tuple(int(x.strip()) for x in HIDDEN_LAYER_SIZES.split(','))
      model = MLPRegressor(hidden_layer_sizes=layers, activation=ACTIVATION, max_iter=int(MAX_ITER), random_state=42)
      
      model.fit(X_train, y_train)
      
      y_pred = model.predict(X_test)
      y_proba = None
      feature_importances = None
      
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          full_pipeline = model
          
      training_history = {"status": "success", "n_iter": model.n_iter_, "loss": float(model.loss_)}
      task_type = "regression"
      print("[MODEL] MLP Regressor entraîné avec succès.")
  else:
      print("[ERREUR] Données d'entraînement introuvables.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_mlp_regressor" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_mlp_regressor" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_mlp_regressor)" stroke="#8b5cf6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#8b5cf6" opacity="0.15" filter="url(#glow_model_mlp_regressor)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#a78bfa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L40 30 L60 70 M30 50 L50 50 M80 30 v40 M100 30 v40" />  </g>  <path d="M 120 50 L 550 50" stroke="#8b5cf6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="350" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="500" cy="50" r="4" fill="#8b5cf6"/></svg>
_metadata:
  bloc: 3
  section: "6_neural_networks"
