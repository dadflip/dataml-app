id: "model_mlp_classifier"
name: "Model: Multi-Layer Perceptron (Classifier)"
description: "Réseau de neurones artificiel standard (fully connected) via Scikit-Learn pour la classification."
category: "Réseaux de Neurones"
tags: ["model", "classification", "tabular", "sklearn", "mlp", "neural-network"]
applies_to: ["Tabulaire"]
when_to_use: "Baseline rapide pour les réseaux de neurones sans sortir l'artillerie lourde PyTorch."
pros:
  - "Intégré dans Sklearn, pas de dépendances complexes."
cons:
  - "Pas d'accélération GPU."
hyperparameters:
  hidden_layer_sizes: "Tailles des couches séparées par des virgules (ex: '100, 50')."
  activation: "Fonction d'activation ('relu', 'tanh', 'logistic')."
  max_iter: "Nombre max d'époques (ex: 200)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.neural_network import MLPClassifier
  try:
      from imblearn.pipeline import Pipeline
  except ImportError:
      from sklearn.pipeline import Pipeline

  # [PARAM: HIDDEN_LAYER_SIZES | string]
  HIDDEN_LAYER_SIZES = "100, 50"
  # [/PARAM]

  # [PARAM: ACTIVATION | string]
  ACTIVATION = "relu"
  # [/PARAM]

  # [PARAM: MAX_ITER | number]
  MAX_ITER = 200
  # [/PARAM]

  print(f"[MODEL] Initialisation MLP Classifier (layers={HIDDEN_LAYER_SIZES}, act={ACTIVATION})...")
  
  # Parse hidden layer sizes
  try:
      layer_sizes = tuple([int(x.strip()) for x in str(HIDDEN_LAYER_SIZES).split(",") if x.strip()])
  except ValueError:
      layer_sizes = (100, 50)
      
  model = MLPClassifier(hidden_layer_sizes=layer_sizes, activation=ACTIVATION, max_iter=int(MAX_ITER), random_state=42)

  if 'preprocessor' in locals() and preprocessor is not None:
      if hasattr(preprocessor, 'steps'):
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          full_pipeline = Pipeline(steps=[('preprocessor', preprocessor), ('model', model)])
  else:
      full_pipeline = Pipeline(steps=[('model', model)])

  y_pred = None
  y_proba = None
  feature_importances = None
  training_history = {}
  task_type = "classification"
  meta = {}

  if 'X_train' in locals() and 'y_train' in locals() and 'X_test' in locals():
      print("[MODEL] Entraînement du pipeline avec MLP Classifier...")
      full_pipeline.fit(X_train, y_train)
      y_pred = full_pipeline.predict(X_test)
      if hasattr(full_pipeline, "predict_proba"):
          y_proba = full_pipeline.predict_proba(X_test)
      
      training_history = {"status": "success", "fitted_on": len(X_train)}
illustration_mermaid: |
  graph TD
    A[Input Data: X_train, y_train] --> B(Preprocessing Pipeline)
    B --> C[MLP Classifier Model]
    
    subgraph Multi-Layer Perceptron
        C --> D[Input Layer]
        D --> E[Hidden Layer 1]
        E --> F[Hidden Layer 2]
        F --> G[Output Layer with Softmax / Sigmoid]
        G --> H[Loss Calculation: Cross-Entropy]
        H -.->|Backpropagation| D
    end
    
    H --> I[Fitted MLP Weights]
    
    X_test[Input Data: X_test] --> J(Preprocessing Pipeline)
    J --> K[Apply MLP Weights]
    I -.-> K
    K --> L([y_pred: Class Predictions])
    K --> M([y_proba: Class Probabilities])
    
    style C fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
    style I fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
    style L fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
    style M fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff

_metadata:
  bloc: 3
  section: "6_neural_networks"
