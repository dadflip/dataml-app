id: "model_t_sne"
name: "Model: t-SNE"
description: "T-distributed Stochastic Neighbor Embedding. Réduction de dimension non-linéaire excellente pour la visualisation 2D."
category: "Réduction de Dimension"
tags: ["model", "dimensionality-reduction", "tabular", "sklearn", "tsne", "visualization"]
applies_to: ["Tabulaire"]
when_to_use: "Pour visualiser des clusters cachés dans un dataset haute dimension."
pros:
  - "Visualisations magnifiques."
cons:
  - "Très lent."
  - "Transductif (impossible d'appliquer sur de nouvelles données test)."
hyperparameters:
  n_components: "Nombre de dimensions (ex: 2)."
  perplexity: "Densité locale vs globale (ex: 30)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.manifold import TSNE

  # [PARAM: N_COMPONENTS | number]
  N_COMPONENTS = 2
  # [/PARAM]

  # [PARAM: PERPLEXITY | number]
  PERPLEXITY = 30
  # [/PARAM]

  print(f"[MODEL] Exécution t-SNE (components={N_COMPONENTS}, perplexity={PERPLEXITY})...")
  if 'X_test' in locals():
      # t-SNE s'applique directement sur ce qu'on veut visualiser (X_test ici, souvent on sample)
      # Attention si X_test est trop grand ça peut être très lent
      if len(X_test) > 5000:
          print("Attention: t-SNE peut être lent sur plus de 5000 lignes.")
          
      model = TSNE(n_components=int(N_COMPONENTS), perplexity=float(PERPLEXITY), random_state=42)
      
      # fit_transform direct
      y_pred = model.fit_transform(X_test)
      y_proba = None
      feature_importances = None
      
      # Pas de pipeline exportable pour t-SNE
      from sklearn.preprocessing import FunctionTransformer
      wrapped = FunctionTransformer(lambda X: model.fit_transform(X))
      
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', wrapped)])
      else:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=[('model', wrapped)])
          
      training_history = {"status": "success"}
      task_type = "dimensionality_reduction"
      print("[MODEL] t-SNE exécuté avec succès.")
  else:
      print("[ERREUR] Données de test introuvables.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_t_sne" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_t_sne" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_t_sne)" stroke="#8b5cf6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#8b5cf6" opacity="0.15" filter="url(#glow_model_t_sne)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#a78bfa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L40 30 L60 70 M30 50 L50 50 M80 30 v40 M100 30 v40" />  </g>  <path d="M 120 50 L 550 50" stroke="#8b5cf6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="350" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="500" cy="50" r="4" fill="#8b5cf6"/></svg>
_metadata:
  bloc: 3
  section: "5_clustering_automl_and_nn"
