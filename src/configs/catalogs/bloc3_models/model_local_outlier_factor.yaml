id: "model_local_outlier_factor"
name: "Model: Local Outlier Factor (LOF)"
description: "Détection d'anomalies basée sur la densité locale de chaque point."
category: "Détection d'Anomalies (Non-Supervisé)"
tags: ["model", "anomaly-detection", "tabular", "sklearn", "lof"]
applies_to: ["Tabulaire"]
when_to_use: "Pour détecter des anomalies contextuelles (un point très éloigné de son petit voisinage)."
pros:
  - "Excellent pour les anomalies locales cachées dans des clusters denses."
cons:
  - "Très lent sur les gros datasets (calcul de distances)."
hyperparameters:
  n_neighbors: "Nombre de voisins K (ex: 20)."
  contamination: "Proportion d'anomalies attendue (ex: 0.05)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.neighbors import LocalOutlierFactor
  from sklearn.base import BaseEstimator, OutlierMixin
  try:
      from imblearn.pipeline import Pipeline
  except ImportError:
      from sklearn.pipeline import Pipeline

  # [PARAM: N_NEIGHBORS | number]
  N_NEIGHBORS = 20
  # [/PARAM]

  # [PARAM: CONTAMINATION | number]
  CONTAMINATION = 0.05
  # [/PARAM]

  print(f"[MODEL] Initialisation Local Outlier Factor (k={N_NEIGHBORS}, cont={CONTAMINATION})...")

  class LOFAnomalyWrapper(BaseEstimator, OutlierMixin):
      def __init__(self, n_neighbors=20, contamination=0.1):
          self.n_neighbors = n_neighbors
          self.contamination = contamination
          self.model = LocalOutlierFactor(
              n_neighbors=self.n_neighbors, 
              contamination=self.contamination, 
              novelty=True
          )
          
      def fit(self, X, y=None):
          self.model.fit(X)
          return self
          
      def predict(self, X):
          preds = self.model.predict(X)
          return np.where(preds == -1, 1, 0)
          
      def decision_function(self, X):
          return self.model.decision_function(X)
          
      def predict_proba(self, X):
          scores = -self.model.decision_function(X) 
          scores = np.clip(scores, 0, None)
          proba = 1 / (1 + np.exp(-scores)) # Pseudo-probability of anomaly
          return np.column_stack([1 - proba, proba])

  model = LOFAnomalyWrapper(n_neighbors=int(N_NEIGHBORS), contamination=float(CONTAMINATION))

  if 'preprocessor' in locals() and preprocessor is not None:
      if hasattr(preprocessor, 'steps'):
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          full_pipeline = Pipeline(steps=[('preprocessor', preprocessor), ('model', model)])
  else:
      full_pipeline = Pipeline(steps=[('model', model)])

  y_pred = None
  y_proba = None
  feature_importances = None
  training_history = {}
  task_type = "anomaly_detection"
  meta = {}

  if 'X_train' in locals() and 'X_test' in locals():
      print("[MODEL] Entraînement du pipeline avec LOF...")
      full_pipeline.fit(X_train)
      y_pred = full_pipeline.predict(X_test)
      y_proba = full_pipeline.predict_proba(X_test)
      
      training_history = {"status": "success", "anomalies_found": int(np.sum(y_pred))}
illustration_mermaid: |
  graph TD
    A[Input Data: X_train] --> B(Preprocessing Pipeline)
    B --> C[Local Outlier Factor Model]
    
    subgraph LOF Process
        C --> D[Compute k-nearest neighbors]
        D --> E[Calculate Local Reachability Density]
        E --> F[Compare Density with Neighbors]
        F --> G[Assign LOF Score to X_train]
    end
    
    G --> H[Fitted LOF Space]
    
    X_test[Input Data: X_test] --> I(Preprocessing Pipeline)
    I --> J[Map X_test into LOF Space]
    H -.-> J
    J --> K{Is Density << Neighbors?}
    K -- Yes --> L[Novelty / Anomaly class 1]
    K -- No --> M[Normal class 0]
    
    style C fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
    style H fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
    style L fill:#ef4444,stroke:#dc2626,stroke-width:2px,color:#fff
    style M fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff

_metadata:
  bloc: 3
  section: "5_clustering_automl_and_nn"
