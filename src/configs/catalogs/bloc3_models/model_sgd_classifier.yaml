id: "model_sgd_classifier"
name: "Model: SGD Classifier"
description: "Modèle linéaire avec descente de gradient stochastique."
category: "Modèles Linéaires"
tags: ["model", "classification", "tabular", "sklearn", "linear", "online-learning"]
applies_to: ["Tabulaire"]
when_to_use: "Excellente alternative à LogisticRegression ou LinearSVC sur les jeux de données géants (n_samples > 100k)."
pros:
  - "Très rapide sur les matrices creuses (NLP)."
cons:
  - "Sensible au scaling et aux hyperparamètres (learning_rate)."
hyperparameters:
  loss: "Fonction de perte ('hinge' donne un Linear SVM, 'log_loss' donne une Logistic Regression)."
  penalty: "Pénalité ('l2', 'l1', 'elasticnet')."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.linear_model import SGDClassifier

  # [PARAM: LOSS | string]
  LOSS = "hinge"
illustration_mermaid: |
    graph LR
      Data([Features]) --> Model((Machine Learning))
      Model --> Pred([Predictions])
      style Model fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style Data fill:#f8fafc,stroke:#94a3b8,stroke-width:1px
      style Pred fill:#f8fafc,stroke:#94a3b8,stroke-width:1px

_metadata:
  bloc: 3
  section: "1_classification_tabulaire"
