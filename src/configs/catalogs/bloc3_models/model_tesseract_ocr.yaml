id: "model_tesseract_ocr"
name: "Model: Tesseract OCR (Pytesseract)"
description: "Moteur OCR open-source classique de Google. Extrait le texte d'images propres."
category: "Vision par Ordinateur (OCR)"
tags: ["model", "vision", "ocr", "tesseract", "pytesseract"]
applies_to: ["Images"]
when_to_use: "Pour numériser des documents scannés très propres, des factures ou des PDF avec fond blanc."
pros:
  - "Très rapide sur CPU."
  - "Excellent sur documents scannés propres."
cons:
  - "Mauvais sur images naturelles ou floues."
  - "Nécessite d'installer le binaire système Tesseract-OCR."
hyperparameters:
  lang: "Langue principale (ex: 'fra' pour français, 'eng' pour anglais)."
code_template: |
  import pandas as pd
  import numpy as np
  try:
      import pytesseract
      from PIL import Image
  except ImportError:
      pytesseract = None
      print("[ERREUR] pytesseract n'est pas installé. Lancez : !pip install pytesseract pillow")

  # [PARAM: LANG | string]
  LANG = "fra"
  # [/PARAM]

  print(f"[MODEL] Extraction de texte avec Tesseract OCR (lang={LANG})...")
  if 'X_test' in locals() and pytesseract is not None:
      images = list(X_test)
      
      all_preds = []
      print("Passage de Tesseract sur les images...")
      for img in images:
          try:
              # pytesseract prend un array numpy ou PIL Image
              if isinstance(img, str):
                  pil_img = Image.open(img)
              else:
                  pil_img = Image.fromarray(img.astype('uint8')) if hasattr(img, 'astype') else img
                  
              text = pytesseract.image_to_string(pil_img, lang=LANG)
              all_preds.append(text.strip())
          except Exception as e:
              all_preds.append(f"[ERREUR_OCR: {e}]")
              
      y_pred = np.array(all_preds)
      y_proba = None
      feature_importances = None
      
      # Dummy wrapper
      from sklearn.preprocessing import FunctionTransformer
      model = FunctionTransformer(lambda X: [pytesseract.image_to_string(i) for i in X])
      full_pipeline = model
          
      training_history = {"status": "success", "type": "tesseract_ocr"}
      task_type = "information_extraction"
      print("[MODEL] Tesseract OCR terminé.")
  else:
      print("[ERREUR] Données introuvables ou pytesseract manquant.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_tesseract_ocr" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_tesseract_ocr" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_tesseract_ocr)" stroke="#8b5cf6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#8b5cf6" opacity="0.15" filter="url(#glow_model_tesseract_ocr)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#a78bfa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L40 30 L60 70 M30 50 L50 50 M80 30 v40 M100 30 v40" />  </g>  <path d="M 120 50 L 550 50" stroke="#8b5cf6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="350" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="500" cy="50" r="4" fill="#8b5cf6"/></svg>
_metadata:
  bloc: 3
  section: "4_vision_and_audio"
