id: "model_audio_spectrogram_cnn"
name: "Model: Audio Spectrogram CNN"
description: |
  Modèle de classification audio personnalisé utilisant PyTorch. Il convertit d'abord les signaux audio bruts en spectrogrammes Mel (images 2D) à l'aide de la bibliothèque Librosa, puis applique un réseau de neurones convolutif (CNN) pour extraire les caractéristiques et effectuer la classification.
category: "Multimédia & Audio"
tags: ["model", "audio", "classification", "pytorch", "cnn", "spectrogram"]
applies_to: ["Audio"]
when_to_use: |
  Parfait pour les tâches de classification audio spécialisées (ex. détection de bruits environnementaux, cris d'animaux) lorsque vous souhaitez construire et entraîner un modèle léger et transparent depuis zéro, sans dépendre de modèles pré-entraînés lourds.
use_cases:
  - "Détection d'événements sonores spécifiques (bris de verre, alarmes, aboiements)."
  - "Classification de la parole ou d'instruments de musique sur des petits datasets."
  - "Prototypage rapide d'un modèle audio sur mesure avant de passer à des transformeurs."
pros:
  - "Architecture légère et rapide à entraîner comparativement aux modèles basés sur des Transformers (comme Wav2Vec2)."
  - "Très explicable et facile à modifier (ajout de couches, changement des dimensions)."
  - "Utilise la représentation temps-fréquence (spectrogramme Mel) très efficace pour capturer l'information audio."
cons:
  - "Nécessite la bibliothèque 'librosa' pour le prétraitement du signal."
  - "Gère moins bien les dépendances temporelles longues comparé aux RNN/LSTM ou Transformers."
  - "Les performances peuvent être limitées si le jeu de données d'entraînement est très restreint (pas de pré-entraînement)."
hyperparameters:
  epochs: "Nombre d'époques d'entraînement (ex: 5)."
code_template: |
  import numpy as np
  import pandas as pd
  import sys
  
  # [PARAM: EPOCHS | number]
  EPOCHS = 5
  # [/PARAM]
  
  print(f"[MODEL] Entraînement Audio Spectrogram CNN (epochs={EPOCHS})...")
  
  # Default fallback
  model = None
  full_pipeline = None
  y_pred = None
  y_proba = None
  feature_importances = None
  training_history = {}
  task_type = "classification"
  meta = {"model_name": "Audio Spectrogram CNN"}

  try:
      import torch
      import torch.nn as nn
      import torch.optim as optim
      from torch.utils.data import TensorDataset, DataLoader
      import librosa
  except ImportError as e:
      print(f"[ERREUR] Packages manquants pour Audio CNN: {e}. Installez torch, torchvision, librosa.")
      torch = None

  if 'X_train' in locals() and 'y_train' in locals() and 'X_test' in locals() and torch is not None:
      # Assume X_train is a list of 1D numpy arrays (audio signals) or a 2D numpy array (samples, time)
      # We convert them to Mel spectrograms
      def audio_to_mel(audio_data, sr=22050, n_mels=128):
          mels = []
          for sig in audio_data:
              # if sig is already 2D or 3D, we might just reshape
              if isinstance(sig, np.ndarray) and sig.ndim >= 2:
                  # Force resize/pad or assume already spectrogram
                  pass
              else:
                  # Convert to mel spectrogram
                  S = librosa.feature.melspectrogram(y=np.array(sig, dtype=float), sr=sr, n_mels=n_mels)
                  S_dB = librosa.power_to_db(S, ref=np.max)
                  mels.append(S_dB)
          
          if len(mels) == 0:
              return np.array([])
              
          # Pad or truncate to max length for batching
          max_len = max(m.shape[1] for m in mels)
          mels_padded = np.array([np.pad(m, ((0, 0), (0, max_len - m.shape[1]))) for m in mels])
          # Shape: (batch, n_mels, time) -> add channel dim for CNN -> (batch, 1, n_mels, time)
          return np.expand_dims(mels_padded, axis=1)

      print("[MODEL] Conversion de l'audio en spectrogrammes Mel...")
      X_tr_mel = audio_to_mel(X_train)
      X_te_mel = audio_to_mel(X_test)
      
      # Determine num_classes
      labels_tr = np.array(y_train)
      unique_classes = np.unique(labels_tr)
      num_classes = len(unique_classes)
      
      # Map labels to 0..C-1
      label_map = {lbl: i for i, lbl in enumerate(unique_classes)}
      y_tr_mapped = np.array([label_map[l] for l in labels_tr])
      
      X_tensor = torch.tensor(X_tr_mel, dtype=torch.float32)
      y_tensor = torch.tensor(y_tr_mapped, dtype=torch.long)
      
      dataset = TensorDataset(X_tensor, y_tensor)
      loader = DataLoader(dataset, batch_size=32, shuffle=True)
      
      class AudioCNN(nn.Module):
          def __init__(self, num_classes):
              super(AudioCNN, self).__init__()
              self.conv1 = nn.Conv2d(1, 16, kernel_size=3, padding=1)
              self.pool = nn.MaxPool2d(2, 2)
              self.conv2 = nn.Conv2d(16, 32, kernel_size=3, padding=1)
              self.fc1 = nn.Linear(32 * (X_tr_mel.shape[2] // 4) * (X_tr_mel.shape[3] // 4), 128)
              self.fc2 = nn.Linear(128, num_classes)
              self.relu = nn.ReLU()
              self.dropout = nn.Dropout(0.3)
              
          def forward(self, x):
              x = self.pool(self.relu(self.conv1(x)))
              x = self.pool(self.relu(self.conv2(x)))
              x = x.view(x.size(0), -1)
              x = self.relu(self.fc1(x))
              x = self.dropout(x)
              x = self.fc2(x)
              return x

      device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
      try:
          model = AudioCNN(num_classes).to(device)
      except Exception as e:
          # If dimension calculation fails due to variable length, fallback to adaptive pooling
          class AdaptiveAudioCNN(nn.Module):
              def __init__(self, num_classes):
                  super(AdaptiveAudioCNN, self).__init__()
                  self.conv1 = nn.Conv2d(1, 16, kernel_size=3, padding=1)
                  self.pool = nn.MaxPool2d(2, 2)
                  self.conv2 = nn.Conv2d(16, 32, kernel_size=3, padding=1)
                  self.adapt_pool = nn.AdaptiveAvgPool2d((4, 4))
                  self.fc1 = nn.Linear(32 * 4 * 4, 128)
                  self.fc2 = nn.Linear(128, num_classes)
                  self.relu = nn.ReLU()
                  
              def forward(self, x):
                  x = self.pool(self.relu(self.conv1(x)))
                  x = self.pool(self.relu(self.conv2(x)))
                  x = self.adapt_pool(x)
                  x = x.view(x.size(0), -1)
                  x = self.relu(self.fc1(x))
                  x = self.fc2(x)
                  return x
          model = AdaptiveAudioCNN(num_classes).to(device)
          
      criterion = nn.CrossEntropyLoss()
      optimizer = optim.Adam(model.parameters(), lr=0.001)
      
      print("[MODEL] Entraînement du modèle PyTorch...")
      model.train()
      for epoch in range(int(EPOCHS)):
          running_loss = 0.0
          for inputs, labels in loader:
              inputs, labels = inputs.to(device), labels.to(device)
              optimizer.zero_grad()
              outputs = model(inputs)
              loss = criterion(outputs, labels)
              loss.backward()
              optimizer.step()
              running_loss += loss.item()
              
      # Inference
      model.eval()
      X_te_tensor = torch.tensor(X_te_mel, dtype=torch.float32)
      test_dataset = TensorDataset(X_te_tensor)
      test_loader = DataLoader(test_dataset, batch_size=32, shuffle=False)
      
      all_preds = []
      all_probs = []
      with torch.no_grad():
          for (inputs,) in test_loader:
              inputs = inputs.to(device)
              outputs = model(inputs)
              probs = torch.softmax(outputs, dim=1)
              _, preds = torch.max(outputs, 1)
              all_preds.extend(preds.cpu().numpy())
              all_probs.extend(probs.cpu().numpy())
              
      # Reverse mapping
      inv_label_map = {v: k for k, v in label_map.items()}
      y_pred = np.array([inv_label_map[p] for p in all_preds])
      y_proba = np.array(all_probs)
      
      full_pipeline = model
      training_history = {"status": "success", "epochs_trained": int(EPOCHS)}

math_theory: |
  Le CNN pour Spectrogramme Audio transforme les signaux audio 1D bruts en représentations temps-fréquence 2D (spectrogrammes Mel) pour appliquer des réseaux de neurones convolutifs (CNN). 
  
  1. **Génération du Spectrogramme** : La transformée de Fourier à court terme (STFT) calcule l'énergie $\textcolor{#3b82f6}{S(t, f)}$ du signal $\textcolor{#10b981}{x}$ au cours du temps. Un banc de filtres Mel $M$ est appliqué, suivi d'une mise à l'échelle logarithmique :
  $$
  \textcolor{#fbbf24}{X_{mel}} = \log(M \cdot |\text{STFT}(\textcolor{#10b981}{x})|^2) 
  $$
  
  2. **Propagation Avant du CNN** : Le spectrogramme 2D $\textcolor{#fbbf24}{X_{mel}}$ est passé à travers des couches convolutives avec des poids $\textcolor{#8b5cf6}{W_c}$, du pooling, et des couches denses avec des poids $\textcolor{#ef4444}{W_d}$ :
  $$
  \textcolor{#3b82f6}{h} = \text{Pool}(\text{ReLU}(\textcolor{#8b5cf6}{W_c} \ast \textcolor{#fbbf24}{X_{mel}} + b_c)) 
  $$
  $$
  \textcolor{#10b981}{y_{pred}} = \text{Softmax}(\textcolor{#ef4444}{W_d} \cdot \text{Flatten}(\textcolor{#3b82f6}{h}) + b_d) 
  $$
  
  3. **Fonction de Perte** : La perte d'entropie croisée (Cross-Entropy) est utilisée pour optimiser les poids par rapport aux vraies étiquettes de classe $\textcolor{#8b5cf6}{y}$ :
  $$
  \mathcal{L} = - \sum_{i=1}^{C} \textcolor{#8b5cf6}{y_i} \log(\textcolor{#10b981}{y_{pred, i}}) 
  $$
math_illustration_svg: |
  <svg viewBox="0 0 800 400" xmlns="http://www.w3.org/2000/svg">
    <!-- Raw Audio -->
    <path d="M30 200 Q40 150 50 200 T70 200 T90 200 T110 200 T130 180 T150 220 T170 200" fill="none" stroke="#10b981" stroke-width="3"/>
    <text x="100" y="240" font-family="sans-serif" font-size="14" font-weight="bold" fill="#10b981" text-anchor="middle">Raw Audio x</text>
    
    <path d="M180 200 L210 200" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrow)"/>
    
    <!-- Spectrogram -->
    <rect x="220" y="160" width="80" height="80" fill="#fef3c7" stroke="#fbbf24" stroke-width="3" rx="4"/>
    <!-- Grid inside spectrogram -->
    <line x1="220" y1="180" x2="300" y2="180" stroke="#fde68a" stroke-width="2"/>
    <line x1="220" y1="200" x2="300" y2="200" stroke="#fde68a" stroke-width="2"/>
    <line x1="220" y1="220" x2="300" y2="220" stroke="#fde68a" stroke-width="2"/>
    <line x1="246" y1="160" x2="246" y2="240" stroke="#fde68a" stroke-width="2"/>
    <line x1="273" y1="160" x2="273" y2="240" stroke="#fde68a" stroke-width="2"/>
    <text x="260" y="260" font-family="sans-serif" font-size="14" font-weight="bold" fill="#fbbf24" text-anchor="middle">Mel-Spectrogram X_mel</text>

    <path d="M310 200 L340 200" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrow)"/>

    <!-- CNN Layers -->
    <rect x="350" y="150" width="40" height="100" fill="#f3e8ff" stroke="#8b5cf6" stroke-width="3" rx="4"/>
    <rect x="360" y="140" width="40" height="100" fill="#f3e8ff" stroke="#8b5cf6" stroke-width="3" rx="4"/>
    <rect x="370" y="130" width="40" height="100" fill="#f3e8ff" stroke="#8b5cf6" stroke-width="3" rx="4"/>
    <text x="380" y="270" font-family="sans-serif" font-size="14" font-weight="bold" fill="#8b5cf6" text-anchor="middle">Conv2D (W_c)</text>
    
    <path d="M420 200 L450 200" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrow)"/>
    
    <!-- Feature Map h -->
    <rect x="460" y="170" width="30" height="60" fill="#eff6ff" stroke="#3b82f6" stroke-width="3" rx="2"/>
    <rect x="470" y="160" width="30" height="60" fill="#eff6ff" stroke="#3b82f6" stroke-width="3" rx="2"/>
    <text x="475" y="250" font-family="sans-serif" font-size="14" font-weight="bold" fill="#3b82f6" text-anchor="middle">Features h</text>

    <path d="M510 200 L540 200" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrow)"/>

    <!-- Dense Layers -->
    <rect x="550" y="120" width="20" height="160" fill="#fee2e2" stroke="#ef4444" stroke-width="3" rx="2"/>
    <text x="560" y="300" font-family="sans-serif" font-size="14" font-weight="bold" fill="#ef4444" text-anchor="middle">Dense (W_d)</text>

    <path d="M580 200 L610 200" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrow)"/>
    
    <!-- Output -->
    <circle cx="650" cy="180" r="10" fill="#d1fae5" stroke="#10b981" stroke-width="2"/>
    <circle cx="650" cy="200" r="10" fill="#d1fae5" stroke="#10b981" stroke-width="2"/>
    <circle cx="650" cy="220" r="10" fill="#d1fae5" stroke="#10b981" stroke-width="2"/>
    <text x="650" y="250" font-family="sans-serif" font-size="14" font-weight="bold" fill="#10b981" text-anchor="middle">y_pred</text>

    <defs>
      <marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto"><path d="M0 0 L10 5 L0 10 z" fill="#94a3b8"/></marker>
    </defs>
  </svg>
illustration_mermaid: |
    flowchart LR
      Raw(["Signal Audio Brut"])
      Preproc{"Librosa\n(Mel-Spectrogramme)"}
      Spec(["Tensor 2D\n(Image)"])
      C1{"Conv2d + ReLU\nMaxPool2d"}
      C2{"Conv2d + ReLU\nMaxPool2d"}
      Flat{"Aplatissement"}
      FC1{"Couche Linéaire\n(128)"}
      FC2{"Couche Linéaire\n(Classes)"}
      Out(["Classes Prédites"])
      
      Raw --> Preproc
      Preproc --> Spec
      Spec --> C1
      C1 --> C2
      C2 --> Flat
      Flat --> FC1
      FC1 --> FC2
      FC2 --> Out
      
      style Raw fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
      style Preproc fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style Spec fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
      style C1 fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style C2 fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style Flat fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style FC1 fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style FC2 fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
      style Out fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff

_metadata:
  bloc: 3
  section: "4_vision_and_audio"
