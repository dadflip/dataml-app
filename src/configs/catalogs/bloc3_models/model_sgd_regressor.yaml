id: "model_sgd_regressor"
name: "Model: SGD Regressor"
description: "Régression Linéaire résolue par descente de gradient stochastique."
category: "Modèles Linéaires"
tags: ["model", "regression", "tabular", "sklearn", "linear", "online-learning"]
applies_to: ["Tabulaire"]
when_to_use: "Jeux de données massifs (> 100k lignes) où LinearRegression crashe la RAM."
pros:
  - "Peut s'entraîner en batchs (partial_fit)."
cons:
  - "Très sensible au scaling."
hyperparameters:
  loss: "Fonction de perte ('squared_error', 'huber', 'epsilon_insensitive')."
  penalty: "Régularisation ('l2', 'l1', 'elasticnet')."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.linear_model import SGDRegressor

  # [PARAM: LOSS | string]
  LOSS = "squared_error"
illustration_mermaid: |
    graph LR
      Data([Features]) --> Model((Machine Learning))
      Model --> Pred([Predictions])
      style Model fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style Data fill:#f8fafc,stroke:#94a3b8,stroke-width:1px
      style Pred fill:#f8fafc,stroke:#94a3b8,stroke-width:1px

_metadata:
  bloc: 3
  section: "2_regression_tabulaire"
