id: "model_arima"
name: "Model: ARIMA Forecaster"
description: "Autoregressive Integrated Moving Average. Modèle statistique classique pour les séries temporelles."
category: "Modèles Séries Temporelles"
tags: ["model", "time-series", "forecasting", "statsmodels", "arima"]
applies_to: ["Séries Temporelles"]
when_to_use: "Pour des séries temporelles univariées avec une dépendance linéaire forte dans le temps."
pros:
  - "Très interprétable."
cons:
  - "Nécessite que la série soit stationnaire."
hyperparameters:
  p: "Ordre Auto-Régressif (AR)."
  d: "Ordre de Différenciation (I)."
  q: "Ordre Moyenne Mobile (MA)."
code_template: |
  import pandas as pd
  import numpy as np
  try:
      from statsmodels.tsa.arima.model import ARIMA
  except ImportError:
      ARIMA = None
      print("[ERREUR] statsmodels n'est pas installé. Lancez : !pip install statsmodels")

  # [PARAM: ORDER_P | number]
  ORDER_P = 1
  # [/PARAM]

  # [PARAM: ORDER_D | number]
  ORDER_D = 1
  # [/PARAM]

  # [PARAM: ORDER_Q | number]
  ORDER_Q = 1
  # [/PARAM]

  print(f"[MODEL] Entraînement ARIMA (p={ORDER_P}, d={ORDER_D}, q={ORDER_Q})...")
  if 'y_train' in locals() and 'X_test' in locals() and ARIMA is not None:
      # ARIMA s'entraîne sur y_train uniquement (univarié classique)
      y_tr = np.array(y_train)
      
      try:
          model = ARIMA(y_tr, order=(int(ORDER_P), int(ORDER_D), int(ORDER_Q)))
          model_fit = model.fit()
          
          # Inférence (on prédit autant d'étapes que la taille de X_test)
          steps = len(X_test) if hasattr(X_test, '__len__') else 10
          y_pred = model_fit.forecast(steps=steps)
          
          y_proba = None
          feature_importances = None
          
          full_pipeline = model_fit
              
          training_history = {"status": "success", "aic": model_fit.aic}
          task_type = "forecasting"
          print("[MODEL] Modèle ARIMA entraîné avec succès.")
      except Exception as e:
          print(f"[ERREUR] ARIMA a échoué. Les données doivent être une série continue 1D. Détail: {e}")
  else:
      print("[ERREUR] y_train introuvable ou module statsmodels non installé.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_arima" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_arima" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_arima)" stroke="#8b5cf6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#8b5cf6" opacity="0.15" filter="url(#glow_model_arima)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#a78bfa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L40 30 L60 70 M30 50 L50 50 M80 30 v40 M100 30 v40" />  </g>  <path d="M 120 50 L 550 50" stroke="#8b5cf6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="350" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="500" cy="50" r="4" fill="#8b5cf6"/></svg>
_metadata:
  bloc: 3
  section: "3_nlp_and_timeseries"
