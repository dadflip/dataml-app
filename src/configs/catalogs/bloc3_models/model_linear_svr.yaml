id: "model_linear_svr"
name: "Model: Linear SVR"
description: "Support Vector Regressor à noyau strictement linéaire."
category: "Modèles Basés sur la Distance / Marges"
tags: ["model", "regression", "tabular", "sklearn", "svm", "linear"]
applies_to: ["Tabulaire"]
when_to_use: "Régression SVM rapide sur de grands datasets textuels ou hautement dimensionnels."
pros:
  - "Très rapide (implémentation liblinear)."
cons:
  - "Uniquement linéaire."
hyperparameters:
  C: "Paramètre de régularisation (ex: 1.0)."
  epsilon: "Marge de tolérance d'erreur (ex: 0.0)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.svm import LinearSVR
  try:
      from imblearn.pipeline import Pipeline
  except ImportError:
      from sklearn.pipeline import Pipeline

  # [PARAM: C_PARAM | number]
  C_PARAM = 1.0
  # [/PARAM]

  # [PARAM: EPSILON | number]
  EPSILON = 0.0
  # [/PARAM]

  print(f"[MODEL] Initialisation Linear SVR (C={C_PARAM}, epsilon={EPSILON})...")

  model = LinearSVR(C=float(C_PARAM), epsilon=float(EPSILON), random_state=42, dual="auto")

  if 'preprocessor' in locals() and preprocessor is not None:
      if hasattr(preprocessor, 'steps'):
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          full_pipeline = Pipeline(steps=[('preprocessor', preprocessor), ('model', model)])
  else:
      full_pipeline = Pipeline(steps=[('model', model)])

  y_pred = None
  y_proba = None
  feature_importances = None
  training_history = {}
  task_type = "regression"
  meta = {}

  if 'X_train' in locals() and 'y_train' in locals() and 'X_test' in locals():
      print("[MODEL] Entraînement du pipeline avec Linear SVR...")
      full_pipeline.fit(X_train, y_train)
      y_pred = full_pipeline.predict(X_test)
      
      if hasattr(model, 'coef_'):
          coefs = np.abs(model.coef_)
          if 'feature_names_out' in locals() and feature_names_out is not None and len(coefs) == len(feature_names_out):
              feature_importances = pd.Series(coefs, index=feature_names_out)
          else:
              feature_importances = pd.Series(coefs)
              
      training_history = {"status": "success", "fitted_on": len(X_train)}
illustration_mermaid: |
  graph TD
    A[Input Data: X_train, y_train] --> B(Preprocessing Pipeline)
    B --> C[Linear SVR Model]
    
    subgraph LinearSVR Training
        C --> D[Linear Transformation: y = wx + b]
        D --> E{Epsilon-Tube Margin}
        E --> F[Penalty C applied to points outside tube]
        F --> G[Optimize Weights via Coordinate Descent]
    end
    
    G --> H[Fitted Model Hyperplane]
    
    X_test[Input Data: X_test] --> I(Preprocessing Pipeline)
    I --> J[Apply Learned Weights]
    H -.-> J
    J --> K([y_pred: Continuous Predictions])
    
    style C fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
    style H fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
    style K fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff

_metadata:
  bloc: 3
  section: "2_regression_tabulaire"
