id: "model_kmeans"
name: "Model: K-Means Clustering"
description: "Algorithme non-supervisé de partitionnement en K groupes (clusters) homogènes."
category: "Clustering (Non-Supervisé)"
tags: ["model", "clustering", "tabular", "sklearn", "kmeans"]
applies_to: ["Tabulaire"]
when_to_use: "Pour segmenter une base de clients ou trouver des profils types sans variables cibles (y)."
pros:
  - "Très rapide et intuitif."
cons:
  - "Nécessite de connaître K à l'avance. Sensible aux outliers et aux variables non scalées."
hyperparameters:
  n_clusters: "Nombre de clusters K à former (ex: 3)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.cluster import KMeans

  # [PARAM: N_CLUSTERS | number]
  N_CLUSTERS = 3
  # [/PARAM]

  print(f"[MODEL] Entraînement K-Means (k={N_CLUSTERS})...")
  # K-Means est non-supervisé, il n'a pas besoin de y_train
  if 'X_train' in locals() and 'X_test' in locals():
      model = KMeans(n_clusters=int(N_CLUSTERS), random_state=42, n_init="auto")
      model.fit(X_train)
      
      y_pred = model.predict(X_test)
      y_proba = None
      feature_importances = None
      
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=[('model', model)])
          
      training_history = {"status": "success", "inertia": float(model.inertia_)}
      task_type = "clustering"
      print("[MODEL] K-Means entraîné avec succès.")
  else:
      print("[ERREUR] Données d'entraînement introuvables.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_kmeans" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_kmeans" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_kmeans)" stroke="#8b5cf6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#8b5cf6" opacity="0.15" filter="url(#glow_model_kmeans)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#a78bfa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L40 30 L60 70 M30 50 L50 50 M80 30 v40 M100 30 v40" />  </g>  <path d="M 120 50 L 550 50" stroke="#8b5cf6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="350" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="500" cy="50" r="4" fill="#8b5cf6"/></svg>
_metadata:
  bloc: 3
  section: "5_clustering_automl_and_nn"
