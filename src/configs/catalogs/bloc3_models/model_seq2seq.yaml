id: "model_seq2seq"
name: "Model: Sequence-to-Sequence (Seq2Seq)"
description: "Architecture de Deep Learning de type Encodeur-Décodeur (souvent LSTM ou GRU) conçue pour modéliser des séquences, permettant de transformer une séquence d'entrée de taille variable en une séquence de sortie."
category: "Attention & Transformers"
tags: ["model", "nlp", "generative", "pytorch", "seq2seq", "deep-learning"]
applies_to: ["Séquences", "Texte"]
when_to_use: "Particulièrement adapté pour les problèmes où l'entrée et la sortie sont des séquences de longueurs différentes, comme la traduction ou la génération de texte."
use_cases:
  - "Traduction automatique entre deux langues (ex: anglais vers français)."
  - "Résumé de texte automatique à partir de longs documents."
  - "Génération de réponses conversationnelles dans des systèmes de chatbots."
pros:
  - "Très grande flexibilité pour gérer des séquences de longueurs distinctes (asynchrones)."
  - "Capture efficacement les dépendances temporelles avec des cellules récurrentes (LSTM)."
  - "Peut être amélioré par un mécanisme d'attention pour les très longues séquences."
cons:
  - "Entraînement gourmand en ressources et lent (goulot d'étranglement séquentiel)."
  - "Perte d'informations sur de très longues séquences si utilisé sans mécanisme d'attention (oubli de l'encodeur)."
  - "Complexité de mise au point (tuning des états cachés, teacher forcing, etc.)."
hyperparameters:
  hidden_size: "Taille de l'état caché (ex: 256)."
  epochs: "Nombre d'époques (ex: 10)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.pipeline import Pipeline
  from sklearn.base import BaseEstimator, RegressorMixin
  try:
      import torch
      import torch.nn as nn
      import torch.optim as optim
      from torch.utils.data import DataLoader, TensorDataset
  except ImportError:
      torch = None
      print("[ERREUR] torch n'est pas installé.")

  # [PARAM: HIDDEN_SIZE | number]
  HIDDEN_SIZE = 256
  # [/PARAM]

  # [PARAM: EPOCHS | number]
  EPOCHS = 10
  # [/PARAM]

  task_type = "generative"
  meta = {
      "model_name": "Seq2Seq (LSTM)",
      "framework": "pytorch",
      "task_type": task_type
  }

  full_pipeline = None
  model = None
  y_pred = None
  y_proba = None
  feature_importances = None
  training_history = []

  if torch is not None:
      # Prepare data
      if 'preprocessor' in globals() and preprocessor is not None:
          X_train_processed = preprocessor.fit_transform(X_train)
          X_test_processed = preprocessor.transform(X_test)
      else:
          X_train_processed = X_train.values if isinstance(X_train, pd.DataFrame) else X_train
          X_test_processed = X_test.values if isinstance(X_test, pd.DataFrame) else X_test
          
      if hasattr(X_train_processed, "toarray"):
          X_train_processed = X_train_processed.toarray()
      if hasattr(X_test_processed, "toarray"):
          X_test_processed = X_test_processed.toarray()

      X_tr_np = np.array(X_train_processed, dtype=np.float32)
      X_te_np = np.array(X_test_processed, dtype=np.float32)

      if len(X_tr_np.shape) == 2:
          X_tr_np = X_tr_np.reshape((X_tr_np.shape[0], 1, X_tr_np.shape[1]))
          X_te_np = X_te_np.reshape((X_te_np.shape[0], 1, X_te_np.shape[1]))

      in_feat = X_tr_np.shape[2]
      hs = int(HIDDEN_SIZE)

      class Encoder(nn.Module):
          def __init__(self, input_dim, hidden_dim):
              super().__init__()
              self.lstm = nn.LSTM(input_dim, hidden_dim, batch_first=True)
          def forward(self, x):
              _, (h, c) = self.lstm(x)
              return h, c

      class Decoder(nn.Module):
          def __init__(self, output_dim, hidden_dim):
              super().__init__()
              self.lstm = nn.LSTM(output_dim, hidden_dim, batch_first=True)
              self.fc = nn.Linear(hidden_dim, output_dim)
          def forward(self, x, h, c):
              out, (h, c) = self.lstm(x, (h, c))
              return self.fc(out), h, c

      class Seq2Seq(nn.Module):
          def __init__(self, encoder, decoder):
              super().__init__()
              self.encoder = encoder
              self.decoder = decoder
          def forward(self, src, trg):
              h, c = self.encoder(src)
              out, _, _ = self.decoder(trg, h, c)
              return out

      encoder = Encoder(in_feat, hs)
      decoder = Decoder(in_feat, hs)
      model = Seq2Seq(encoder, decoder)
      device = torch.device("cuda" if torch.cuda.is_available() else "mps" if torch.backends.mps.is_available() else "cpu")
      model = model.to(device)

      optimizer = optim.Adam(model.parameters(), lr=0.001)
      criterion = nn.MSELoss()

      # Auto-encoding : on reconstruit l'entrée
      dataset = TensorDataset(torch.tensor(X_tr_np), torch.tensor(X_tr_np))
      loader = DataLoader(dataset, batch_size=32, shuffle=True)

      model.train()
      for epoch in range(int(EPOCHS)):
          epoch_loss = 0.0
          for bx, by in loader:
              bx, by = bx.to(device), by.to(device)
              optimizer.zero_grad()
              outputs = model(bx, by)
              loss = criterion(outputs, by)
              loss.backward()
              optimizer.step()
              epoch_loss += loss.item()
          training_history.append({"epoch": epoch+1, "loss": epoch_loss/len(loader)})

      model.eval()
      with torch.no_grad():
          xt = torch.tensor(X_te_np).to(device)
          y_pred = model(xt, xt).cpu().numpy()
          
          # Flatten to match test set
          y_pred = y_pred.reshape(y_pred.shape[0], -1)

      class Seq2SeqWrapper(BaseEstimator, RegressorMixin):
          def __init__(self, model, device):
              self.model = model
              self.device = device
          def predict(self, X):
              self.model.eval()
              if hasattr(X, "toarray"): X = X.toarray()
              X_np = np.array(X, dtype=np.float32)
              if len(X_np.shape) == 2:
                  X_np = X_np.reshape((X_np.shape[0], 1, X_np.shape[1]))
              X_tensor = torch.tensor(X_np).to(self.device)
              with torch.no_grad():
                  preds = self.model(X_tensor, X_tensor).cpu().numpy()
              return preds.reshape(preds.shape[0], -1)

      wrapped_model = Seq2SeqWrapper(model, device)

      if 'preprocessor' in globals() and preprocessor is not None:
          full_pipeline = Pipeline([
              ('preprocessor', preprocessor),
              ('model', wrapped_model)
          ])
      else:
          full_pipeline = Pipeline([
              ('model', wrapped_model)
          ])
math_theory: |
  L'architecture Seq2Seq repose sur la modélisation conjointe de deux processus utilisant des cellules LSTM/GRU.
  L'encodeur traite la séquence d'entrée $\textcolor{#10b981}{X}$ pour produire une séquence d'états cachés $\textcolor{#3b82f6}{h_t}$, où l'état final sert de vecteur de contexte $\textcolor{#8b5cf6}{c}$.
  
  $$
  \textcolor{#3b82f6}{h_t} = \text{LSTM}_{enc}(\textcolor{#10b981}{x_t}, \textcolor{#3b82f6}{h_{t-1}}) 
  $$

  $$
  \textcolor{#8b5cf6}{c} = \textcolor{#3b82f6}{h_T} 
  $$
  
  Le décodeur utilise ce contexte pour générer itérativement la séquence de sortie $\textcolor{#fbbf24}{y_t}$, en s'appuyant sur son propre état caché $\textcolor{#ef4444}{s_t}$.
  
  $$
  \textcolor{#ef4444}{s_t} = \text{LSTM}_{dec}(\textcolor{#fbbf24}{y_{t-1}}, \textcolor{#ef4444}{s_{t-1}}, \textcolor{#8b5cf6}{c}) 
  $$

  $$
  P(\textcolor{#fbbf24}{y_t}) = \text{Softmax}(\text{Linear}(\textcolor{#ef4444}{s_t})) 
  $$
math_illustration_svg: |
  <svg viewBox="0 0 800 400" xmlns="http://www.w3.org/2000/svg">
    <!-- Encoder -->
    <rect x="50" y="150" width="80" height="60" rx="10" fill="#3b82f6" />
    <text x="90" y="185" fill="white" font-family="sans-serif" font-size="20" text-anchor="middle">LSTM</text>
    <rect x="180" y="150" width="80" height="60" rx="10" fill="#3b82f6" />
    <text x="220" y="185" fill="white" font-family="sans-serif" font-size="20" text-anchor="middle">LSTM</text>
    <rect x="310" y="150" width="80" height="60" rx="10" fill="#3b82f6" />
    <text x="350" y="185" fill="white" font-family="sans-serif" font-size="20" text-anchor="middle">LSTM</text>
    
    <!-- Context Vector -->
    <rect x="310" y="50" width="80" height="40" rx="5" fill="#8b5cf6" />
    <text x="350" y="75" fill="white" font-family="sans-serif" font-size="16" text-anchor="middle">c (Context)</text>
    
    <!-- Decoder -->
    <rect x="450" y="150" width="80" height="60" rx="10" fill="#ef4444" />
    <text x="490" y="185" fill="white" font-family="sans-serif" font-size="20" text-anchor="middle">LSTM</text>
    <rect x="580" y="150" width="80" height="60" rx="10" fill="#ef4444" />
    <text x="620" y="185" fill="white" font-family="sans-serif" font-size="20" text-anchor="middle">LSTM</text>
    <rect x="710" y="150" width="80" height="60" rx="10" fill="#ef4444" />
    <text x="750" y="185" fill="white" font-family="sans-serif" font-size="20" text-anchor="middle">LSTM</text>

    <!-- Inputs -->
    <circle cx="90" cy="270" r="20" fill="#10b981" />
    <text x="90" y="276" fill="white" font-family="sans-serif" font-size="16" text-anchor="middle">x1</text>
    <circle cx="220" cy="270" r="20" fill="#10b981" />
    <text x="220" y="276" fill="white" font-family="sans-serif" font-size="16" text-anchor="middle">x2</text>
    <circle cx="350" cy="270" r="20" fill="#10b981" />
    <text x="350" y="276" fill="white" font-family="sans-serif" font-size="16" text-anchor="middle">x3</text>

    <!-- Outputs -->
    <circle cx="490" cy="60" r="20" fill="#fbbf24" />
    <text x="490" y="66" fill="white" font-family="sans-serif" font-size="16" text-anchor="middle">y1</text>
    <circle cx="620" cy="60" r="20" fill="#fbbf24" />
    <text x="620" y="66" fill="white" font-family="sans-serif" font-size="16" text-anchor="middle">y2</text>
    <circle cx="750" cy="60" r="20" fill="#fbbf24" />
    <text x="750" y="66" fill="white" font-family="sans-serif" font-size="16" text-anchor="middle">y3</text>
    
    <!-- Arrows -->
    <path d="M 90 250 L 90 210" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 220 250 L 220 210" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 350 250 L 350 210" stroke="black" stroke-width="2" marker-end="url(#arrow)" />

    <path d="M 130 180 L 180 180" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 260 180 L 310 180" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 350 150 L 350 90" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 390 70 L 490 70 L 490 150" stroke="black" stroke-width="2" fill="none" marker-end="url(#arrow)" />
    <path d="M 390 180 L 450 180" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 530 180 L 580 180" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 660 180 L 710 180" stroke="black" stroke-width="2" marker-end="url(#arrow)" />

    <path d="M 490 150 L 490 80" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 620 150 L 620 80" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    <path d="M 750 150 L 750 80" stroke="black" stroke-width="2" marker-end="url(#arrow)" />
    
    <!-- Defs for arrow -->
    <defs>
      <marker id="arrow" markerWidth="10" markerHeight="10" refX="9" refY="3" orient="auto" markerUnits="strokeWidth">
        <path d="M0,0 L0,6 L9,3 z" fill="black" />
      </marker>
    </defs>
  </svg>
illustration_mermaid: |
  flowchart LR
    SeqIn(["Input Sequence"]) --> Enc{"Encoder LSTM"}
    Enc --> State(["Context Vector"])
    State --> Dec{"Decoder LSTM"}
    Dec --> SeqOut(["Output Sequence"])
    style Enc fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
    style Dec fill:#3b82f6,stroke:#2563eb,stroke-width:2px,color:#fff
    style SeqIn fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
    style State fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
    style SeqOut fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff

_metadata:
  bloc: 3
  section: "6_neural_networks"
