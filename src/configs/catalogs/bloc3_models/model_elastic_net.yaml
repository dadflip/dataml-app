id: "model_elastic_net"
name: "Model: ElasticNet"
description: "Régression ElasticNet combinant les pénalités L1 et L2."
category: "Model"
tags: ["model", "regression", "elasticnet"]
applies_to: ["Tabulaire"]
when_to_use: "Quand vous voulez un modèle linéaire robuste évitant l'overfitting."
pros:
  - "Gère bien la multicolinéarité."
cons:
  - "Hypothèse de linéarité forte."
hyperparameters:
  alpha: "Force de régularisation globale."
  l1_ratio: "Ratio L1 vs L2 (0 = Ridge, 1 = Lasso)."
code_template: |
  import pandas as pd
  from sklearn.linear_model import ElasticNet
  
  # [PARAM: ALPHA | number]
  ALPHA = 1.0
  # [/PARAM]
  
  # [PARAM: L1_RATIO | number]
  L1_RATIO = 0.5
  # [/PARAM]
  
  print(f"[MODEL] Entraînement ElasticNet (Alpha={ALPHA}, L1_ratio={L1_RATIO})...")
  
  if 'X_train' in locals() and 'X_test' in locals() and 'y_train' in locals():
      model = ElasticNet(alpha=float(ALPHA), l1_ratio=float(L1_RATIO), random_state=42)
      model.fit(X_train, y_train)
      y_pred = model.predict(X_test)
      try:
          y_proba = model.predict_proba(X_test)
      except Exception:
          y_proba = None
      
      feature_importances = None
      if hasattr(model, "coef_"):
          feature_importances = dict(zip(X_train.columns, model.coef_))
          
      full_pipeline = model
      training_history = {"status": "success"}
      task_type = "regression"
      print("[MODEL] ElasticNet entraîné avec succès.")
  else:
      print("[ERREUR] Données d'entraînement introuvables.")

illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_elastic_net" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_elastic_net" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_elastic_net)" stroke="#8b5cf6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#8b5cf6" opacity="0.15" filter="url(#glow_model_elastic_net)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#a78bfa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L40 30 L60 70 M30 50 L50 50 M80 30 v40 M100 30 v40" />  </g>  <path d="M 120 50 L 550 50" stroke="#8b5cf6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="350" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="500" cy="50" r="4" fill="#8b5cf6"/></svg>
_metadata:
  bloc: 3
  section: "3_models"
