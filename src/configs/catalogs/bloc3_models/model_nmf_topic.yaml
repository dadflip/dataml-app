id: "model_nmf_topic"
name: "Model: Non-Negative Matrix Factorization (NMF)"
description: "Algorithme d'algèbre linéaire pour la modélisation thématique (Topic Modeling)."
category: "Modèles NLP (Entraînés)"
tags: ["model", "nlp", "unsupervised", "topic-modeling", "sklearn", "nmf"]
applies_to: ["Texte"]
when_to_use: "Alternative à LDA, souvent meilleure et plus rapide lorsqu'on utilise des embeddings TF-IDF plutôt que CountVectorizer."
pros:
  - "Très performant avec TF-IDF."
  - "Rapide."
cons:
  - "Ne produit pas de vraies probabilités comme le LDA."
hyperparameters:
  n_components: "Nombre de topics (ex: 10)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.decomposition import NMF

  # [PARAM: N_COMPONENTS | number]
  N_COMPONENTS = 10
  # [/PARAM]

  print(f"[MODEL] Entraînement NMF Topic Modeling (n_topics={N_COMPONENTS})...")
  if 'X_train' in locals() and 'X_test' in locals():
      model = NMF(n_components=int(N_COMPONENTS), init='nndsvda', random_state=42)
      
      X_tr_proc = X_train
      X_te_proc = X_test
      
      if isinstance(X_train, pd.DataFrame) and 'text' in X_train.columns:
          from sklearn.feature_extraction.text import TfidfVectorizer
          tv = TfidfVectorizer(max_features=5000)
          X_tr_proc = tv.fit_transform(X_train['text'].astype(str))
          X_te_proc = tv.transform(X_test['text'].astype(str))
          
      model.fit(X_tr_proc)
      
      topic_dist = model.transform(X_te_proc)
      y_pred = np.argmax(topic_dist, axis=1) # Topic dominant
      y_proba = None
      
      feature_importances = None
      
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=[('model', model)])
          
      training_history = {"status": "success", "reconstruction_err": model.reconstruction_err_}
      task_type = "clustering"
      print("[MODEL] NMF entraîné avec succès.")
  else:
      print("[ERREUR] Données d'entraînement introuvables.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_nmf_topic" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_nmf_topic" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_nmf_topic)" stroke="#8b5cf6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#8b5cf6" opacity="0.15" filter="url(#glow_model_nmf_topic)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#a78bfa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L40 30 L60 70 M30 50 L50 50 M80 30 v40 M100 30 v40" />  </g>  <path d="M 120 50 L 550 50" stroke="#8b5cf6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="350" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="500" cy="50" r="4" fill="#8b5cf6"/></svg>
_metadata:
  bloc: 3
  section: "3_nlp_and_timeseries"
