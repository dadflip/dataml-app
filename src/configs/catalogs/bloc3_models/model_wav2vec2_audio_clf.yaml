id: "model_wav2vec2_audio_clf"
name: "Model: Wav2Vec2 Audio Classifier"
description: "Classification audio Zero-Shot ou Feature Extraction via le modèle de Facebook (HuggingFace)."
category: "Multimédia & Audio"
tags: ["model", "audio", "classification", "transformers", "huggingface", "wav2vec2"]
applies_to: ["Audio"]
when_to_use: "Pour classifier des émotions vocales, des commandes vocales ou extraire des features d'un signal audio."
pros:
  - "Très performant sur la voix humaine."
cons:
  - "Entrées très lourdes (tableaux de fréquences à 16kHz)."
hyperparameters: {}
code_template: |
  import pandas as pd
  import numpy as np
  try:
      from transformers import pipeline
  except ImportError:
      pipeline = None
      print("[ERREUR] transformers n'est pas installé. Lancez : !pip install transformers librosa")

  print(f"[MODEL] HuggingFace Audio Classification...")
  if 'X_test' in locals() and pipeline is not None:
      # X_test contient des chemins vers des fichiers .wav ou des arrays numpy (rate 16000)
      audios = list(X_test)
      
      print("Chargement du classifieur audio...")
      # Modèle générique de classification d'émotions
      classifier = pipeline("audio-classification", model="ehcalabres/wav2vec2-lg-xlsr-en-speech-emotion-recognition")
      
      print("Inférence en cours...")
      results = []
      for a in audios:
          try:
              res = classifier(a)
              results.append(res[0]['label'])
          except Exception as e:
              results.append("Erreur")
              
      y_pred = np.array(results)
      y_proba = None
      feature_importances = None
      model = classifier
      
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          full_pipeline = model
          
      training_history = {"status": "success"}
      task_type = "classification"
      print("[MODEL] Inférence audio terminée avec succès.")
  else:
      print("[ERREUR] Données introuvables ou transformers non installé.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_wav2vec2_audio_clf" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_wav2vec2_audio_clf" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_wav2vec2_audio_clf)" stroke="#10b981" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#10b981" opacity="0.15" filter="url(#glow_model_wav2vec2_audio_clf)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#34d399" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 Q50 20 80 50 T140 50" />  </g>  <path d="M 120 50 L 550 50" stroke="#10b981" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#10b981"/>  <circle cx="350" cy="50" r="4" fill="#10b981"/>  <circle cx="500" cy="50" r="4" fill="#10b981"/></svg>
_metadata:
  bloc: 3
  section: "4_vision_and_audio"
