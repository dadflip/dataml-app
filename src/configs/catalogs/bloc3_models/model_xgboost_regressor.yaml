id: "model_xgboost_regressor"
name: "Model: XGBoost Regressor"
description: "Extreme Gradient Boosting appliqué à la régression."
category: "Modèles Basés sur les Arbres"
tags: ["model", "regression", "tabular", "xgboost", "ensemble", "boosting"]
applies_to: ["Tabulaire"]
when_to_use: "Quand vous voulez maximiser le R2 et minimiser le RMSE sur des données tabulaires."
pros:
  - "Performance pure très élevée."
  - "Gère les NaNs en interne."
cons:
  - "Installation de xgboost requise."
hyperparameters:
  n_estimators: "Nombre d'arbres (ex: 100)."
  learning_rate: "Taux d'apprentissage (ex: 0.1)."
  max_depth: "Profondeur maximale (ex: 6)."
code_template: |
  import pandas as pd
  import numpy as np
  try:
      from xgboost import XGBRegressor
  except ImportError:
      XGBRegressor = None
      print("[ERREUR] xgboost n'est pas installé. Lancez : !pip install xgboost")

  # [PARAM: N_ESTIMATORS | number]
  N_ESTIMATORS = 100
  # [/PARAM]

  # [PARAM: LEARNING_RATE | number]
  LEARNING_RATE = 0.1
  # [/PARAM]

  # [PARAM: MAX_DEPTH | number]
  MAX_DEPTH = 6
  # [/PARAM]

  print(f"[MODEL] Entraînement XGBoost Regressor (n={N_ESTIMATORS}, lr={LEARNING_RATE}, depth={MAX_DEPTH})...")
  if 'X_train' in locals() and 'y_train' in locals() and 'X_test' in locals() and XGBRegressor is not None:
      model = XGBRegressor(
          n_estimators=int(N_ESTIMATORS), 
          learning_rate=float(LEARNING_RATE), 
          max_depth=int(MAX_DEPTH),
          random_state=42
      )
      model.fit(X_train, y_train)
      
      y_pred = model.predict(X_test)
      y_proba = None
      
      feature_importances = None
      if 'feature_names_out' in locals() and hasattr(model, 'feature_importances_'):
          if len(model.feature_importances_) == len(feature_names_out):
              feature_importances = pd.Series(model.feature_importances_, index=feature_names_out)
              
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=[('model', model)])
          
      training_history = {"status": "success", "fitted_on": len(X_train)}
      task_type = "regression"
illustration_mermaid: |
    graph TD
      In([Input Features]) --> Tree1(Decision Tree 1)
      Tree1 --> Res1(Compute Residuals)
      Res1 --> Tree2(Decision Tree 2)
      Tree2 --> Res2(Compute Residuals)
      Res2 --> TreeN(Decision Tree N)
      TreeN --> Sum(Sum Weighted Predictions)
      Sum --> Out([Continuous Predictions])
      
      style In fill:#f8fafc,stroke:#94a3b8,stroke-width:2px
      style Tree1 fill:#38bdf8,stroke:#0284c7,stroke-width:2px,color:#fff
      style Tree2 fill:#38bdf8,stroke:#0284c7,stroke-width:2px,color:#fff
      style TreeN fill:#38bdf8,stroke:#0284c7,stroke-width:2px,color:#fff
      style Sum fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style Out fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff

_metadata:
  bloc: 3
  section: "2_regression_tabulaire"
