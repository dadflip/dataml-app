id: "model_pca"
name: "Model: Principal Component Analysis (PCA)"
description: "Réduction de dimension linéaire. Transforme les données en composantes principales orthogonales."
category: "Réduction de Dimension"
tags: ["model", "dimensionality-reduction", "tabular", "sklearn", "pca"]
applies_to: ["Tabulaire"]
when_to_use: "Pour visualiser un dataset de grande dimension en 2D/3D ou supprimer la colinéarité avant un modèle lourd."
pros:
  - "Très rapide."
  - "Préserve la variance globale."
cons:
  - "Linéaire uniquement."
hyperparameters:
  n_components: "Nombre de composantes à garder (ex: 2)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.decomposition import PCA

  # [PARAM: N_COMPONENTS | number]
  N_COMPONENTS = 2
  # [/PARAM]

  print(f"[MODEL] Exécution PCA (components={N_COMPONENTS})...")
  if 'X_train' in locals() and 'X_test' in locals():
      # Note: PCA est souvent utilisé comme Preprocessor, mais on peut l'utiliser ici pour générer y_pred comme coordonnées 2D
      model = PCA(n_components=int(N_COMPONENTS), random_state=42)
      model.fit(X_train)
      
      # y_pred sera les nouvelles coordonnées (matrice au lieu de vecteur)
      y_pred = model.transform(X_test)
      y_proba = None
      
      # La feature importance d'une PCA peut être vue comme la variance expliquée
      feature_importances = pd.Series(model.explained_variance_ratio_, index=[f"PC{i+1}" for i in range(int(N_COMPONENTS))])
      
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=[('model', model)])
          
      training_history = {"status": "success", "explained_variance": sum(model.explained_variance_ratio_)}
      task_type = "dimensionality_reduction"
      print("[MODEL] PCA exécutée avec succès.")
  else:
      print("[ERREUR] Données d'entraînement introuvables.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_pca" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_pca" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_pca)" stroke="#8b5cf6" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#8b5cf6" opacity="0.15" filter="url(#glow_model_pca)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#a78bfa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 70 L40 30 L60 70 M30 50 L50 50 M80 30 v40 M100 30 v40" />  </g>  <path d="M 120 50 L 550 50" stroke="#8b5cf6" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="350" cy="50" r="4" fill="#8b5cf6"/>  <circle cx="500" cy="50" r="4" fill="#8b5cf6"/></svg>
_metadata:
  bloc: 3
  section: "5_clustering_automl_and_nn"
