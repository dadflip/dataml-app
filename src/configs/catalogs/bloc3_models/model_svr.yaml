id: "model_svr"
name: "Model: Support Vector Regressor (SVR)"
description: "Régression à vecteurs de support. Utilise l'astuce du noyau pour des relations non-linéaires."
category: "Modèles Basés sur la Distance / Marges"
tags: ["model", "regression", "tabular", "sklearn", "svm"]
applies_to: ["Tabulaire"]
when_to_use: "Petits datasets non-linéaires où l'erreur est mesurée avec une tolérance (epsilon)."
pros:
  - "Très puissant en dimension modérée."
cons:
  - "Lent sur plus de 10 000 échantillons."
hyperparameters:
  kernel: "Noyau ('rbf', 'poly', 'linear', 'sigmoid')."
  C: "Pénalité."
  epsilon: "Marge de tolérance d'erreur (ex: 0.1)."
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.svm import SVR

  # [PARAM: KERNEL | string]
  KERNEL = "rbf"
illustration_mermaid: |
    graph LR
      Data([Features]) --> Model((Machine Learning))
      Model --> Pred([Predictions])
      style Model fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff
      style Data fill:#f8fafc,stroke:#94a3b8,stroke-width:1px
      style Pred fill:#f8fafc,stroke:#94a3b8,stroke-width:1px

_metadata:
  bloc: 3
  section: "2_regression_tabulaire"
