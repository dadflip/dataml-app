id: "model_gpt2_text_gen"
name: "Model: GPT-2 Text Generation"
description: "Générateur de texte basé sur l'architecture GPT-2 de OpenAI via HuggingFace."
category: "Modèles NLP (Pré-entraînés)"
tags: ["model", "nlp", "text-generation", "transformers", "huggingface", "gpt2", "generative"]
applies_to: ["Texte"]
when_to_use: "Pour prolonger une phrase ou un prompt donné (ex: génération de fausses descriptions ou data augmentation)."
pros:
  - "Très fun et créatif."
cons:
  - "Souvent incohérent sur de longs textes comparé à GPT-4. Ne fait que de l'auto-complétion, pas de chat."
hyperparameters:
  max_length: "Nombre total de tokens désirés (ex: 50)."
code_template: |
  import pandas as pd
  import numpy as np
  try:
      from transformers import pipeline
  except ImportError:
      pipeline = None
      print("[ERREUR] transformers n'est pas installé. Lancez : !pip install transformers")

  # [PARAM: MAX_LENGTH | number]
  MAX_LENGTH = 50
  # [/PARAM]

  print(f"[MODEL] HuggingFace GPT-2 Text Generation (max_length={MAX_LENGTH})...")
  
  model = None
  y_pred = None
  y_proba = None
  feature_importances = None
  training_history = {}
  task_type = "text_generation"
  meta = {"framework": "transformers", "type": "gpt2"}
  full_pipeline = None

  if 'X_test' in locals() and pipeline is not None:
      texts = []
      if isinstance(X_test, pd.DataFrame) and 'text' in X_test.columns:
          texts = X_test['text'].astype(str).tolist()
      elif isinstance(X_test, (pd.Series, np.ndarray, list)):
          texts = [str(x) for x in X_test]
          
      if texts:
          print("Chargement de GPT-2...")
          generator = pipeline("text-generation", model="gpt2")
          
          print("Génération en cours...")
          results = generator(texts, max_new_tokens=int(MAX_LENGTH), num_return_sequences=1)
          
          y_pred = np.array([res[0]['generated_text'] for res in results])
          model = generator
          
          if 'preprocessor' in locals() and preprocessor is not None:
              try: from imblearn.pipeline import Pipeline
              except: from sklearn.pipeline import Pipeline
              full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
          else:
              full_pipeline = model
              
          training_history = {"status": "success", "type": "zero_shot_generative"}

illustration_mermaid: |
  graph TD
    subgraph GPT-2 Text Generation
      In([Input Text Prompt]) --> Tokenizer[BPE Tokenizer]
      Tokenizer --> Tokens[Input Tokens]
      Tokens --> Blocks[Transformer Decoder Blocks x12]
      Blocks --> Logits[Vocabulary Logits]
      Logits --> Search[Greedy/Beam Search]
      Search --> GenTokens[Generated Tokens]
      GenTokens --> Decode[Detokenizer]
      Decode --> Out([Generated Text])
    end
    style In fill:#f8fafc,stroke:#94a3b8,stroke-width:1px
    style Out fill:#10b981,stroke:#059669,stroke-width:2px,color:#fff
    style Blocks fill:#8b5cf6,stroke:#7c3aed,stroke-width:2px,color:#fff

_metadata:
  bloc: 3
  section: "3_nlp_and_timeseries"
