id: "model_whisper_stt"
name: "Model: OpenAI Whisper (Speech-to-Text)"
description: "Reconnaissance vocale universelle et robuste (Speech-to-Text) par OpenAI."
category: "Multimédia & Audio"
tags: ["model", "audio", "speech-to-text", "transformers", "huggingface", "whisper", "zero-shot"]
applies_to: ["Audio"]
when_to_use: "Pour transcrire un fichier audio (.mp3, .wav) en texte pur dans n'importe quelle langue."
pros:
  - "SOTA absolu en transcription."
  - "Gère les accents et le bruit de fond."
cons:
  - "Lent sur CPU."
hyperparameters:
  model_size: "Taille du modèle ('tiny', 'base', 'small')."
code_template: |
  import pandas as pd
  import numpy as np
  try:
      from transformers import pipeline
  except ImportError:
      pipeline = None
      print("[ERREUR] transformers n'est pas installé. Lancez : !pip install transformers")

  # [PARAM: MODEL_SIZE | string]
  MODEL_SIZE = "tiny"
  # [/PARAM]

  print(f"[MODEL] Transcription audio Whisper (size={MODEL_SIZE})...")
  if 'X_test' in locals() and pipeline is not None:
      audios = list(X_test) # Liste de paths vers des fichiers audio
      
      print(f"Chargement de Whisper ({MODEL_SIZE})...")
      transcriber = pipeline("automatic-speech-recognition", model=f"openai/whisper-{MODEL_SIZE}")
      
      print("Transcription en cours...")
      results = []
      for a in audios:
          try:
              res = transcriber(a)
              results.append(res["text"])
          except Exception as e:
              results.append(f"[ERREUR: {e}]")
              
      y_pred = np.array(results)
      y_proba = None
      feature_importances = None
      model = transcriber
      
      if 'preprocessor' in locals() and preprocessor is not None:
          try: from imblearn.pipeline import Pipeline
          except: from sklearn.pipeline import Pipeline
          full_pipeline = Pipeline(steps=preprocessor.steps + [('model', model)])
      else:
          full_pipeline = model
          
      training_history = {"status": "success", "type": "speech_to_text"}
      task_type = "information_extraction"
      print("[MODEL] Transcription terminée.")
  else:
      print("[ERREUR] Données introuvables ou transformers non installé.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_model_whisper_stt" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_model_whisper_stt" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_model_whisper_stt)" stroke="#10b981" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#10b981" opacity="0.15" filter="url(#glow_model_whisper_stt)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#34d399" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 Q50 20 80 50 T140 50" />  </g>  <path d="M 120 50 L 550 50" stroke="#10b981" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#10b981"/>  <circle cx="350" cy="50" r="4" fill="#10b981"/>  <circle cx="500" cy="50" r="4" fill="#10b981"/></svg>
_metadata:
  bloc: 3
  section: "4_vision_and_audio"
