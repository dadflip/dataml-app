id: eda_custom_flowers_grid
name: 'EDA: Aperçu des Fleurs'
description: Affiche quelques images de fleurs en lisant les chemins depuis le dataframe.
category: Custom
tags:
- eda
- images
- 102flowers
- custom
code_template: "import matplotlib.pyplot as plt\nfrom PIL import Image\nimport os\n\
  \n# [PARAM: NUM_IMAGES | number]\nNUM_IMAGES = 16\n# [/PARAM]\n\nprint(\"[EDA] Aperçu\
  \ des images...\")\nif 'df' in locals() and 'image_path' in df.columns:\n    # S'assure\
  \ que c'est bien des chaînes de caractères (avant transformation tensor)\n    paths\
  \ = df['image_path'].dropna().head(int(NUM_IMAGES)).tolist()\n    \n    if len(paths)\
  \ > 0 and isinstance(paths[0], str):\n        cols = int(len(paths)**0.5)\n    \
  \    rows = (len(paths) + cols - 1) // cols\n        \n        plt.figure(figsize=(10,\
  \ 10))\n        for i, path in enumerate(paths):\n            if os.path.exists(path):\n\
  \                img = Image.open(path).convert('RGB')\n                plt.subplot(rows,\
  \ cols, i + 1)\n                plt.imshow(img)\n                plt.axis('off')\n\
  \        \n        plt.suptitle(f\"Aperçu de {len(paths)} fleurs\")\n        plt.tight_layout()\n\
  \        plt.show()\n    else:\n        print(\"[EDA] Impossible d'afficher les\
  \ images (déjà converties en tenseurs ?)\")\nelse:\n    print(\"[ERREUR] 'df' ou\
  \ colonne 'image_path' introuvable.\")\n"
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_eda_custom_flowers_grid" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_eda_custom_flowers_grid" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_eda_custom_flowers_grid)" stroke="#64748b" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#64748b" opacity="0.15" filter="url(#glow_eda_custom_flowers_grid)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#94a3b8" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 L100 50 M60 20 L60 80" />  </g>  <path d="M 120 50 L 550 50" stroke="#64748b" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#64748b"/>  <circle cx="350" cy="50" r="4" fill="#64748b"/>  <circle cx="500" cy="50" r="4" fill="#64748b"/></svg>
  width="400" height="200" fill="#0f172a"/><rect x="50" y="30" width="80" height="60"
  rx="4" fill="#f472b6"/><rect x="160" y="30" width="80" height="60" rx="4" fill="#a78bfa"/><rect
  x="270" y="30" width="80" height="60" rx="4" fill="#38bdf8"/><rect x="50" y="110"
  width="80" height="60" rx="4" fill="#fbbf24"/><rect x="160" y="110" width="80" height="60"
  rx="4" fill="#34d399"/><rect x="270" y="110" width="80" height="60" rx="4" fill="#f87171"/></svg>
_metadata:
  bloc: -1
  section: 0_custom_project
