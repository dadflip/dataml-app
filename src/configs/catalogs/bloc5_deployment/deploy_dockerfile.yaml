id: "deploy_dockerfile"
name: "Deploy: FastAPI Dockerfile"
description: "Génère un Dockerfile et un requirements.txt pour conteneuriser l'API FastAPI du modèle."
category: "Containerization"
tags: ["deploy", "docker", "fastapi", "container"]
applies_to: ["Classification", "Regression"]
when_to_use: "Pour déployer le modèle sur n'importe quel cloud provider (AWS ECS, Google Cloud Run, Azure Container Apps)."
pros:
  - "Standard de l'industrie pour le déploiement de micro-services."
cons:
  - "Nécessite que Docker soit installé pour builder."
code_template: |
  import os

  deployment_artifacts = {}

  print("[DEPLOY] Génération des fichiers Docker...")
  try:
      os.makedirs("outputs/deployment", exist_ok=True)
      
      # 1. requirements.txt
      reqs = """fastapi==0.103.1
  uvicorn==0.23.2
  scikit-learn
  pandas
  numpy
  pydantic
  joblib
  """
      req_path = "outputs/deployment/requirements.txt"
      with open(req_path, "w") as f:
          f.write(reqs)
          
      # 2. Dockerfile
      dockerfile = """FROM python:3.9-slim
  
  WORKDIR /app
  
  # Installation des dépendances
  COPY requirements.txt .
  RUN pip install --no-cache-dir -r requirements.txt
  
  # Copie des fichiers locaux (modèle + serveur API)
  # Assurez-vous d'avoir généré server.py et model.joblib d'abord
  COPY . .
  
  # Exposition du port
  EXPOSE 8000
  
  # Commande de lancement
  CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]
  """
      docker_path = "outputs/deployment/Dockerfile"
      with open(docker_path, "w") as f:
          f.write(dockerfile)
          
      deployment_artifacts["dockerfile"] = docker_path
      deployment_artifacts["requirements"] = req_path
      
      print(f"[DEPLOY] Fichiers Docker générés.")
      print(f"[DEPLOY] Pour builder : cd outputs/deployment && docker build -t dataml_model_api .")
      print(f"[DEPLOY] Pour run : docker run -p 8000:8000 dataml_model_api")
  except Exception as e:
      print(f"[DEPLOY ERREUR] Docker : {str(e)}")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_deploy_dockerfile" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_deploy_dockerfile" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_deploy_dockerfile)" stroke="#ef4444" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#ef4444" opacity="0.15" filter="url(#glow_deploy_dockerfile)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#f87171" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M50 20 L80 80 L20 80 Z" />  </g>  <path d="M 120 50 L 550 50" stroke="#ef4444" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#ef4444"/>  <circle cx="350" cy="50" r="4" fill="#ef4444"/>  <circle cx="500" cy="50" r="4" fill="#ef4444"/></svg>
_metadata:
  bloc: 5
  section: "4_container"
