id: "transform_count_vectorizer"
name: "Transform: Count Vectorizer"
description: "Bag of Words."
category: "4.1 Preprocessing"
tags: ["transform", "nlp", "text", "sklearn", "pipeline"]
applies_to: ["Texte"]
when_to_use: "Modèles Naive Bayes."
pros:
  - "Simple."
cons:
  - "Donne poids aux mots fréquents."
hyperparameters:
  text_col: "Colonne texte"
  max_features: "Nb de features"
code_template: |
  import pandas as pd
  from sklearn.feature_extraction.text import CountVectorizer
  from sklearn.compose import ColumnTransformer

  # [PARAM: TEXT_COL | string]
  TEXT_COL = "text"
  # [/PARAM]

  # [PARAM: MAX_FEATURES | number]
  MAX_FEATURES = 5000
  # [/PARAM]

  print(f"[TRANSFORM] CountVectorizer sur '{TEXT_COL}'...")
  if 'df' in locals():
      if 'pipeline_steps' not in locals(): pipeline_steps = {}
      
      if TEXT_COL in df.columns:
          df[TEXT_COL] = df[TEXT_COL].fillna('')
          
          cv = CountVectorizer(max_features=MAX_FEATURES)
          ct = ColumnTransformer(
              [('cv', cv, TEXT_COL)],
              remainder='passthrough', verbose_feature_names_out=False
          )
          try: ct.set_output(transform="pandas")
          except: pass
          
          step_name = 'count_vectorizer'
          pipeline_steps[step_name] = (step_name, ct)
          
          df = pd.DataFrame(ct.fit_transform(df))
          print(f"[TRANSFORM] CountVectorizer appliqué. Nbre cols: {df.shape[1]}")
      else:
          print("[ERREUR] Colonne introuvable.")
  else:
      print("[ERREUR] 'df' introuvable.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_transform_count_vectorizer" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_transform_count_vectorizer" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_transform_count_vectorizer)" stroke="#10b981" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#10b981" opacity="0.15" filter="url(#glow_transform_count_vectorizer)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#34d399" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 Q50 20 80 50 T140 50" />  </g>  <path d="M 120 50 L 550 50" stroke="#10b981" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#10b981"/>  <circle cx="350" cy="50" r="4" fill="#10b981"/>  <circle cx="500" cy="50" r="4" fill="#10b981"/></svg>
_metadata:
  bloc: 2
  section: "4_preprocessing"
