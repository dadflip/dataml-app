id: "transform_one_hot_encoding"
name: "Transform: One-Hot Encoding"
description: "OHE sur les colonnes catégorielles."
category: "4.1 Preprocessing"
tags: ["transform", "encoding", "tabular", "sklearn", "pipeline"]
applies_to: ["Tabulaire"]
when_to_use: "Indispensable pour les modèles linéaires."
pros:
  - "Empêche l'ordre implicite."
cons:
  - "Augmente la dimension."
hyperparameters:
  drop_first: "True pour éviter la multicolinéarité parfaite."
code_template: |
  import pandas as pd
  from sklearn.preprocessing import OneHotEncoder
  from sklearn.compose import ColumnTransformer

  # [PARAM: DROP_FIRST | boolean]
  DROP_FIRST = True
  # [/PARAM]

  print("[TRANSFORM] One-Hot Encoding...")
  if 'df' in locals():
      if 'pipeline_steps' not in locals(): pipeline_steps = {}
      
      cat_cols = df.select_dtypes(exclude=['number', 'datetime']).columns.tolist()
      if 'target_name' in locals() and target_name in cat_cols:
          cat_cols.remove(target_name)
          
      if len(cat_cols) > 0:
          drop_mode = 'first' if DROP_FIRST else None
          ohe = OneHotEncoder(drop=drop_mode, sparse_output=False, handle_unknown='ignore')
          
          ct = ColumnTransformer(
              [('ohe', ohe, cat_cols)],
              remainder='passthrough', verbose_feature_names_out=False
          )
          try: ct.set_output(transform="pandas")
          except: pass
          
          step_name = 'ohe'
          pipeline_steps[step_name] = (step_name, ct)
          
          df = pd.DataFrame(ct.fit_transform(df))
          # Renommage optionnel si pandas set_output non dispo
          print(f"[TRANSFORM] OHE appliqué au pipeline. Shape: {df.shape}")
      else:
          print("[TRANSFORM] Aucune variable catégorielle.")
  else:
      print("[ERREUR] 'df' introuvable.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_transform_one_hot_encoding" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_transform_one_hot_encoding" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_transform_one_hot_encoding)" stroke="#10b981" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#10b981" opacity="0.15" filter="url(#glow_transform_one_hot_encoding)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#34d399" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 Q50 20 80 50 T140 50" />  </g>  <path d="M 120 50 L 550 50" stroke="#10b981" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#10b981"/>  <circle cx="350" cy="50" r="4" fill="#10b981"/>  <circle cx="500" cy="50" r="4" fill="#10b981"/></svg>
_metadata:
  bloc: 2
  section: "4_preprocessing"
