id: "transform_text_padding"
name: "Transform: Text Sequence Padding"
description: "Padding de séquences d'entiers."
category: "4.1 Preprocessing"
tags: ["transform", "nlp", "text", "pandas", "pipeline"]
applies_to: ["Texte"]
when_to_use: "RNN/LSTM."
pros:
  - "Batching."
cons:
  - "Mémoire."
hyperparameters:
  seq_col: "Colonne list"
  max_len: "Longueur"
code_template: |
  import pandas as pd
  import numpy as np
  from sklearn.preprocessing import FunctionTransformer

  # [PARAM: SEQ_COL | string]
  SEQ_COL = "sequences"
  # [/PARAM]

  # [PARAM: MAX_LEN | number]
  MAX_LEN = 100
  # [/PARAM]

  print(f"[TRANSFORM] Padding sur '{SEQ_COL}'...")
  if 'df' in locals():
      if 'pipeline_steps' not in locals(): pipeline_steps = {}
      
      if SEQ_COL in df.columns:
          def pad_func(X):
              X_df = pd.DataFrame(X) if not isinstance(X, pd.DataFrame) else X
              def _pad(s):
                  if not isinstance(s, (list, np.ndarray)): return s
                  return s[:MAX_LEN] if len(s) > MAX_LEN else list(s) + [0] * (MAX_LEN - len(s))
              X_df[SEQ_COL] = X_df[SEQ_COL].apply(_pad)
              return X_df

          transformer = FunctionTransformer(pad_func)
          
          step_name = 'text_padding'
          pipeline_steps[step_name] = (step_name, transformer)
          
          df = transformer.transform(df)
          print("[TRANSFORM] Padding appliqué au pipeline.")
      else:
          print("[ERREUR] Colonne introuvable.")
  else:
      print("[ERREUR] 'df' introuvable.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_transform_text_padding" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_transform_text_padding" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_transform_text_padding)" stroke="#10b981" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#10b981" opacity="0.15" filter="url(#glow_transform_text_padding)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#34d399" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 Q50 20 80 50 T140 50" />  </g>  <path d="M 120 50 L 550 50" stroke="#10b981" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#10b981"/>  <circle cx="350" cy="50" r="4" fill="#10b981"/>  <circle cx="500" cy="50" r="4" fill="#10b981"/></svg>
_metadata:
  bloc: 2
  section: "4_preprocessing"
