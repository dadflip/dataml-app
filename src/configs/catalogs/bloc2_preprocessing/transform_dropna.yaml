id: "transform_dropna"
name: "Transform: Drop NaNs"
description: "Supprime les lignes contenant des valeurs manquantes. Suivi dans le pipeline via FunctionTransformer."
category: "4.1 Preprocessing"
tags: ["transform", "cleaning", "tabular", "pandas", "pipeline"]
applies_to: ["Tabulaire"]
when_to_use: "Lorsqu'il y a très peu de valeurs manquantes."
pros:
  - "Très rapide et simple."
cons:
  - "Le drop dans un pipeline sklearn est souvent déconseillé pour l'inférence (il vaut mieux imputer)."
hyperparameters:
  subset: "Colonnes spécifiques à vérifier (vide = toutes)."
code_template: |
  import pandas as pd
  from sklearn.preprocessing import FunctionTransformer

  # [PARAM: SUBSET_COLS | string]
  SUBSET_COLS = ""
  # [/PARAM]

  print("[TRANSFORM] Suppression des lignes avec NaNs...")
  if 'df' in locals():
      if 'pipeline_steps' not in locals(): pipeline_steps = {}
      
      def drop_nans_func(X):
          # On vérifie si X est un DataFrame, car dans le pipeline ce sera X_train
          X_df = pd.DataFrame(X) if not isinstance(X, pd.DataFrame) else X
          if SUBSET_COLS.strip():
              cols = [c.strip() for c in SUBSET_COLS.split(',')]
              valid = [c for c in cols if c in X_df.columns]
              return X_df.dropna(subset=valid)
          return X_df.dropna()

      transformer = FunctionTransformer(drop_nans_func)
      
      # Enregistrement dans la pipeline (sans ColumnTransformer car on touche les lignes)
      step_name = 'drop_nans'
      pipeline_steps[step_name] = (step_name, transformer)
      
      # Application locale
      initial_len = len(df)
      df = transformer.transform(df)
      print(f"[TRANSFORM] {initial_len - len(df)} lignes supprimées. Nouvelle taille: {df.shape}")
  else:
      print("[ERREUR] 'df' introuvable.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_transform_dropna" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_transform_dropna" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_transform_dropna)" stroke="#10b981" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#10b981" opacity="0.15" filter="url(#glow_transform_dropna)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#34d399" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 Q50 20 80 50 T140 50" />  </g>  <path d="M 120 50 L 550 50" stroke="#10b981" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#10b981"/>  <circle cx="350" cy="50" r="4" fill="#10b981"/>  <circle cx="500" cy="50" r="4" fill="#10b981"/></svg>
_metadata:
  bloc: 2
  section: "4_preprocessing"
