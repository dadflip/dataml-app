id: "transform_smote"
name: "Transform: SMOTE (Oversampling)"
description: "Génère des exemples synthétiques pour balancer les classes. Requiert imblearn."
category: "4.1 Preprocessing"
tags: ["transform", "imbalance", "tabular", "imblearn", "pipeline"]
applies_to: ["Tabulaire"]
when_to_use: "Classes très déséquilibrées (ex: Fraude)."
pros:
  - "Évite le biais majoritaire."
cons:
  - "Lent."
hyperparameters:
  sampling_strategy: "auto"
code_template: |
  import pandas as pd
  try:
      from imblearn.over_sampling import SMOTE
  except ImportError:
      SMOTE = None
      print("[ERREUR] Installez imbalanced-learn.")

  print("[TRANSFORM] SMOTE...")
  if 'df' in locals() and 'target_name' in locals() and target_name and SMOTE:
      if 'pipeline_steps' not in locals(): pipeline_steps = {}
      
      if target_name in df.columns:
          smote = SMOTE()
          
          # SMOTE est un sampler imblearn. Il s'applique à la fin sur X et y entiers
          step_name = 'smote'
          pipeline_steps[step_name] = (step_name, smote)
          
          X = df.drop(columns=[target_name])
          y = df[target_name]
          
          # Application locale
          # Note: Ne marche que si X ne contient plus de NaNs ou strings
          try:
              X_res, y_res = smote.fit_resample(X, y)
              df = pd.concat([X_res, y_res], axis=1)
              print(f"[TRANSFORM] SMOTE appliqué. Nouvelles lignes : {len(df)}")
          except Exception as e:
              print(f"[ERREUR] SMOTE a échoué (Avez-vous imputé/encodé vos données avant ?) : {e}")
      else:
          print("[ERREUR] Cible non trouvée.")
  else:
      print("[ERREUR] df, cible manquant ou module absent.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_transform_smote" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_transform_smote" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_transform_smote)" stroke="#10b981" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#10b981" opacity="0.15" filter="url(#glow_transform_smote)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#34d399" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 Q50 20 80 50 T140 50" />  </g>  <path d="M 120 50 L 550 50" stroke="#10b981" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#10b981"/>  <circle cx="350" cy="50" r="4" fill="#10b981"/>  <circle cx="500" cy="50" r="4" fill="#10b981"/></svg>
_metadata:
  bloc: 2
  section: "4_preprocessing"
