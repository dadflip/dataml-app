pipeline_contract:
  bloc: 2
  role: "Preprocessing"
  input_variables:
    df: "pd.DataFrame | None"
    target_name: "str | None"
    task_type: "str"
    meta: "dict"
  output_variables:
    X_train: "pd.DataFrame"
    X_test: "pd.DataFrame"
    y_train: "pd.Series"
    y_test: "pd.Series"
    preprocessor: "sklearn.compose.ColumnTransformer | sklearn.pipeline.Pipeline | None"
    feature_names_out: "list[str]"
    task_type: "str"
    meta: "dict"
