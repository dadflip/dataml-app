id: "transform_drop_outliers"
name: "Transform: Z-Score Outliers"
description: "Supprime les lignes aberrantes. Suivi dans le pipeline."
category: "4.1 Preprocessing"
tags: ["transform", "cleaning", "outliers", "tabular", "pipeline"]
applies_to: ["Tabulaire"]
when_to_use: "Pour éliminer les valeurs aberrantes."
pros:
  - "Nettoie les erreurs de mesure."
cons:
  - "Le drop dans un pipeline est risqué pour l'inférence. À utiliser avec précaution."
hyperparameters:
  z_thresh: "Seuil du Z-Score (souvent 3.0)."
code_template: |
  import pandas as pd
  import numpy as np
  from scipy import stats
  from sklearn.preprocessing import FunctionTransformer

  # [PARAM: Z_THRESH | number]
  Z_THRESH = 3.0
  # [/PARAM]

  print(f"[TRANSFORM] Détection et suppression outliers (Z > {Z_THRESH})...")
  if 'df' in locals():
      if 'pipeline_steps' not in locals(): pipeline_steps = {}
      
      def drop_outliers_func(X):
          X_df = pd.DataFrame(X) if not isinstance(X, pd.DataFrame) else X
          num_cols = X_df.select_dtypes(include=[np.number]).columns
          if len(num_cols) > 0:
              z_scores = np.abs(stats.zscore(X_df[num_cols], nan_policy='omit'))
              mask = (z_scores < Z_THRESH) | np.isnan(z_scores)
              return X_df[mask.all(axis=1)]
          return X_df

      transformer = FunctionTransformer(drop_outliers_func)
      
      step_name = 'drop_outliers'
      pipeline_steps[step_name] = (step_name, transformer)
      
      initial_len = len(df)
      df = transformer.transform(df)
      print(f"[TRANSFORM] {initial_len - len(df)} outliers supprimés.")
  else:
      print("[ERREUR] 'df' introuvable.")
illustration_svg: <svg viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">  <defs>    <linearGradient id="grad_transform_drop_outliers" x1="0%" y1="0%" x2="100%" y2="100%">      <stop offset="0%" stop-color="#0f172a" stop-opacity="1" />      <stop offset="100%" stop-color="#1e293b" stop-opacity="1" />    </linearGradient>    <filter id="glow_transform_drop_outliers" x="-20%" y="-20%" width="140%" height="140%">      <feGaussianBlur stdDeviation="4" result="blur" />      <feComposite in="SourceGraphic" in2="blur" operator="over" />    </filter>  </defs>  <rect width="600" height="100" rx="12" fill="url(#grad_transform_drop_outliers)" stroke="#10b981" stroke-width="2" stroke-opacity="0.3"/>  <circle cx="50" cy="50" r="30" fill="#10b981" opacity="0.15" filter="url(#glow_transform_drop_outliers)"/>  <g transform="translate(10, 15) scale(0.7)" fill="none" stroke="#34d399" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">    <path d="M20 50 Q50 20 80 50 T140 50" />  </g>  <path d="M 120 50 L 550 50" stroke="#10b981" stroke-width="1" stroke-dasharray="4 8" opacity="0.5"/>  <circle cx="200" cy="50" r="4" fill="#10b981"/>  <circle cx="350" cy="50" r="4" fill="#10b981"/>  <circle cx="500" cy="50" r="4" fill="#10b981"/></svg>
_metadata:
  bloc: 2
  section: "4_preprocessing"
